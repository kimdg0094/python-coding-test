# Ch6. 1차원 배열 — Codetree 101 스터디 가이드

> 언어 Python3 · 2개 레슨 · 개념 2 / 문제 4

## 📌 챕터 개요

여러 개의 값을 순서대로 담는 리스트(1차원 배열) 자료형을 배웁니다. 리스트의 생성·인덱싱·수정과 주요 메서드(append, insert, remove, pop, sort 등)를 익히고, for문과 리스트 컴프리헨션으로 배열을 순회·가공하는 방법을 학습합니다. 학습 목표는 (1) 리스트에 값을 저장하고 인덱스로 접근·수정하기, (2) 여러 값을 한 번에 입력받아 리스트로 처리하기, (3) for문·컴프리헨션으로 배열을 변환하며 문제를 해결하는 것입니다.

## 📖 개념 정리 (레슨별)

### L1. 1차원 배열 입문

**핵심 개념**
- 리스트는 여러 값을 순서대로 저장하는 변경 가능한(mutable) 자료형이며, 대괄호 `[]`로 정의합니다.
- 정수·문자열·실수 등 서로 다른 자료형을 한 리스트에 함께 담을 수 있습니다.
- 각 요소는 인덱스(위치)를 가지며 인덱스는 0부터 시작하고, 음수 인덱스(-1이 마지막)로도 접근할 수 있습니다.
- 메서드(method)는 특정 자료형이 자신만의 기능을 위해 갖는 특별한 함수로, `리스트.append(x)`처럼 값 뒤에 점(`.`)을 찍고 호출합니다. `print()`·`len()` 같은 일반 함수는 여러 자료형에 두루 쓰입니다.

**문법/사용법**
- 생성: 값 나열 `[1, 2, 3]`, `list("abc")`, `input().split()`(공백 기준 분리)
- 결합/반복: `list1 + list2`(새 리스트 반환, 원본 유지), `list1 * 3`(반복 — 반복 횟수는 정수만 가능, 음수는 사용 불가)
- 접근/수정: `arr[i]`로 읽기, `arr[i] = 값`으로 변경 (범위 초과 시 IndexError)
- 주요 메서드: `append(x)` 끝에 추가, `insert(i, x)` 위치 삽입, `remove(x)` 첫 일치 요소 삭제, `pop(i)` 제거+반환(기본 마지막), `sort()`/`sort(reverse=True)` 정렬, `reverse()` 뒤집기, `count(x)` 개수, `index(x)` 위치 찾기
- 길이: `len(arr)` (메서드가 아닌 함수이므로 리스트뿐 아니라 문자열·튜플·딕셔너리 등 다른 자료형에도 적용 가능)

**예시**
```python
fruits = ["사과", "바나나", "체리"]
print(fruits[0])      # 사과
print(fruits[-1])     # 체리
fruits[1] = "딸기"    # 요소 수정
fruits.append("오렌지")
print(len(fruits))    # 4
nums = [5, 2, 9, 1]
nums.sort()           # [1, 2, 5, 9]
```

**주의·흔한 실수**
- 인덱스는 0부터 시작 — 3번째 요소는 `arr[2]`입니다.
- 범위를 벗어난 인덱스 접근은 `IndexError`, 없는 요소 `remove`나 `index`는 `ValueError`가 발생합니다.
- `+`/`*` 결합·반복은 원본을 바꾸지 않고 새 리스트를 반환합니다. `sort()`·`reverse()`는 원본을 직접 바꿉니다.
- `len`은 함수이므로 `arr.len()`이 아니라 `len(arr)`로 씁니다.

### L2. 1차원 배열 응용

**핵심 개념**
- for문으로 리스트의 각 요소를 순서대로 처리하며 연산·필터링·변환을 수행합니다.
- 여러 정수를 한 줄에 입력받아 리스트로 만드는 관용구를 익힙니다.
- 리스트 컴프리헨션으로 for문 여러 줄을 한 줄로 간결하게 표현합니다.

**문법/사용법**
- 인덱스 순회: `for i in range(len(arr)):` → `arr[i]`로 요소와 위치 모두 활용
- 요소 직접 순회: `for x in arr:` → 인덱스가 불필요할 때 간결
- 여러 정수 입력: `arr = list(map(int, input().split()))`
- 여러 줄 입력을 리스트로: `for _ in range(n): arr.append(input())` 또는 컴프리헨션 `arr = [input() for _ in range(n)]` (N개의 값이 줄마다 따로 주어질 때)
- 관용 변수 `_`: 반복 인덱스를 쓰지 않고 횟수만 필요할 때 `for _ in range(n)`처럼 버리는 변수로 사용
- 리스트 컴프리헨션: `새리스트 = [표현식 for 변수 in 시퀀스]`
- 리스트 펼쳐 출력(언패킹): `print(*[1, 2, 3])`은 `print(1, 2, 3)`과 같아 요소를 공백으로 펼쳐 한 줄에 출력합니다. 구분자는 `print(*arr, sep=",")`처럼 `sep`으로 바꿀 수 있습니다.

**예시**
```python
# 여러 정수 입력받기
nums = list(map(int, input().split()))   # "10 20 30" -> [10, 20, 30]

# for문으로 2배
doubled = []
for x in nums:
    doubled.append(x * 2)

# 컴프리헨션으로 제곱 리스트
squares = [x ** 2 for x in range(10)]    # [0, 1, 4, 9, ...]
```

**주의·흔한 실수**
- 요소 직접 순회(`for x in arr`)에서는 요소의 인덱스에 직접 접근할 수 없으므로, 위치가 필요하면 `range(len(arr))`를 씁니다.
- 입력을 정수로 처리하려면 `map(int, ...)`로 변환해야 합니다. 변환을 빼먹으면 문자열 리스트가 됩니다.
- 컴프리헨션은 `[표현식 for 변수 in 시퀀스]` 순서 — 표현식이 앞, for절이 뒤입니다.
- 출력 형식(공백 구분 등)은 요구사항에 맞춰야 하므로, 계산 후 출력 시 `for x in 리스트: print(x, end=" ")`로 한 값씩 붙여 출력하거나 `print(*리스트)`(아래 언패킹 설명 참고)를 활용합니다. (`" ".join(...)`도 되지만 자세히는 Ch7)

## 🧩 문제 채점용 정리

### 1) 배열의 합 계산하기 (Easy · 10XP · 정답률 93%)

- **요구사항**: 배열 A를 `[3, 1, 4, 5, 9]`로 선언·저장한 뒤, 1번·3번·4번 인덱스에 저장된 값의 합을 출력합니다.
- **입력**: 입력이 주어지지 않습니다.
- **출력**: A의 1번·3번·4번 인덱스 값의 합.
- **예제**: (입력 없음) → `15`
- **제한**: Time Limit 1000 ms / Memory Limit 128 MiB
- **연습 개념**: 리스트 선언·초기화, 인덱싱(0부터 시작)
- **셀프체크 포인트**: 인덱스 1·3·4는 각각 값 1·5·9임을 확인(1+5+9=15). "1번째"가 아니라 "인덱스 1"임에 유의.

```runner
@@SOLUTION
A = [3, 1, 4, 5, 9]
print(A[1] + A[3] + A[4])
@@TESTS
--IN

--OUT
15
@@EXPL
접근 방법: 리스트를 문제에서 준 값 그대로 선언하고, 필요한 인덱스의 요소를 직접 꺼내 더합니다. 입력이 없는 고정값 문제라 계산은 인덱싱만으로 끝납니다.

코드 진행 과정:

- `A = [3, 1, 4, 5, 9]`로 배열을 만듭니다. 인덱스는 0부터라 값은 A[0]=3, A[1]=1, A[2]=4, A[3]=5, A[4]=9입니다.
- 요구된 1·3·4번 인덱스 값은 각각 1, 5, 9이므로 `A[1] + A[3] + A[4]`는 15입니다.
- `print`로 15를 출력합니다.

다시 작성할 때 생각 순서:

- 배열을 그대로 적어 둔다.
- "인덱스 1·3·4"가 몇 번째 값인지 0부터 세어 확인한다(1번째가 아님).
- 세 값을 인덱싱으로 꺼내 더한 뒤 출력한다.
```

### 2) 배열 채우고 출력하기 (Easy · 10XP · 정답률 87%)

- **요구사항**: 문자 10개를 저장하는 배열을 만들어 10개의 문자를 입력받고, 입력 순서의 역순으로 출력합니다.
- **입력**: 첫 줄에 10개의 문자가 공백으로 구분되어 주어집니다.
- **출력**: 입력받은 순서의 역순으로(공백 없이 이어 붙여) 출력합니다.
- **예제**: `L E B R O S C O D E` → `EDOCSORBEL`
- **제한**: Time Limit 1000 ms / Memory Limit 80 MiB
- **연습 개념**: `input().split()` 입력, 리스트 역순 처리(`reverse()` 또는 인덱스 역순회)
- **셀프체크 포인트**: 출력이 공백 없이 붙어 나오는지 확인. 역순은 `arr.reverse()`로 리스트를 뒤집거나 `for i in range(len(arr)-1, -1, -1):`처럼 인덱스를 마지막부터 순회한다. 공백 없이 이어 붙이려면 `print(x, end="")`로 한 문자씩 출력한다. (슬라이싱 `[::-1]`이나 `"".join(...)`도 되지만 자세히는 Ch7)

```runner
@@SOLUTION
arr = input().split()
for i in range(len(arr) - 1, -1, -1):
    print(arr[i], end="")
print()
@@TESTS
--IN
L E B R O S C O D E
--OUT
EDOCSORBEL
--IN
A B C D E F G H I J
--OUT
JIHGFEDCBA
@@EXPL
접근 방법: 공백으로 구분된 문자들을 `split()`으로 리스트에 담고, 마지막 요소부터 첫 요소까지 거꾸로 순회하며 한 문자씩 공백 없이 이어 출력합니다.

코드 진행 과정:

- `input().split()`은 `"L E B R O S C O D E"`를 `["L", "E", "B", ...]`처럼 문자 리스트로 만듭니다(int 변환은 필요 없음 — 문자 그대로 다룸).
- `range(len(arr)-1, -1, -1)`은 인덱스를 9, 8, ..., 1, 0 순으로 만들어 마지막부터 순회합니다.
- `print(arr[i], end="")`로 각 문자를 줄바꿈·공백 없이 붙여 출력합니다.
- 마지막에 `print()`로 줄바꿈을 한 번 넣어 줄을 마칩니다.

다시 작성할 때 생각 순서:

- 먼저 입력을 어떻게 리스트로 만들지 정한다(공백 구분이므로 `split()`).
- 역순 출력이 핵심이니 `range(len-1, -1, -1)`로 뒤에서부터 인덱스를 돈다.
- 공백 없이 붙여야 하므로 `end=""`를 쓰고, 끝에 줄바꿈만 한 번 추가한다.
```

### 3) 제곱하여 출력하기 (Easy · 10XP · 정답률 90%)

- **요구사항**: N개의 원소가 주어지면 각 원소를 제곱하여 입력 순서대로 출력합니다.
- **입력**: 첫 줄에 원소 개수 N, 둘째 줄에 N개의 원소가 공백으로 구분되어 주어집니다.
- **출력**: 각 원소를 제곱한 결과를 입력 순서대로 공백으로 구분하여 출력합니다.
- **예제**: `3 / 1 8 5` → `1 64 25` · `2 / 10 4` → `100 16`
- **제한**: 1≤N≤100, 1≤주어지는 수≤100 · Time Limit 1000 ms / Memory Limit 80 MiB
- **연습 개념**: 여러 정수 입력(`list(map(int, ...))`), for문/컴프리헨션 변환, 공백 구분 출력
- **셀프체크 포인트**: 결과를 공백으로 구분해 한 줄에 출력하는지 확인. `for x in 결과: print(x, end=" ")`처럼 한 값씩 공백을 붙여 출력하면 된다(`print(*결과)`도 가능 — 아래 L2 참고).

```runner
@@SOLUTION
n = int(input())
arr = list(map(int, input().split()))
for x in arr:
    print(x * x, end=" ")
print()
@@TESTS
--IN
3
1 8 5
--OUT
1 64 25
--IN
2
10 4
--OUT
100 16
@@EXPL
접근 방법: 개수 N을 읽은 뒤 정수 리스트를 입력받고, for문으로 각 원소를 제곱해 공백으로 구분하며 한 줄에 출력합니다.

코드 진행 과정:

- `n = int(input())`으로 첫 줄의 개수를 읽습니다(리스트 길이는 이미 입력에 담기므로 계산에는 직접 쓰지 않아도 됩니다).
- `list(map(int, input().split()))`로 둘째 줄을 정수 리스트로 만듭니다 — `map(int, ...)`를 빼면 문자열이 되어 제곱이 안 됩니다.
- `for x in arr`로 요소를 순서대로 돌며 `x * x`를 계산하고, `print(..., end=" ")`로 값 뒤에 공백을 붙여 이어 출력합니다.
- 채점은 줄 끝 공백을 무시하므로 마지막 값 뒤 공백은 문제되지 않습니다. `print()`로 줄을 마칩니다.

다시 작성할 때 생각 순서:

- 입력이 두 줄임을 파악한다: 개수 한 줄, 값 한 줄.
- 값 줄을 정수 리스트로 바꾼다(`map(int, ...)` 잊지 않기).
- 각 원소를 제곱해 공백 구분으로 출력한다(`end=" "` 또는 `print(*결과)`).
```

### 4) 일의 자리 배열 (Easy · 10XP · 정답률 88%)

- **요구사항**: 10 미만 정수 두 개를 첫째·둘째 항으로 두고, 세 번째 항부터는 전전항+전항의 합의 일의 자리(합 % 10)로 채워 총 10개를 차례로 출력합니다.
- **입력**: 첫 줄에 첫째 항, 둘째 항이 공백으로 구분되어 주어집니다.
- **출력**: 첫 줄에 10개의 숫자를 공백으로 구분하여 출력합니다.
- **예제**: `2 5` → `2 5 7 2 9 1 0 1 1 2` (예: 넷째 항은 5+7=12의 일의 자리인 2)
- **제한**: 0≤주어지는 정수≤9 · Time Limit 1000 ms / Memory Limit 80 MiB
- **연습 개념**: 리스트 초기값 설정 후 반복으로 채우기, 인덱스 접근(`arr[i-1]`, `arr[i-2]`), 나머지 연산 `% 10`
- **셀프체크 포인트**: "합의 일의 자리"이므로 `(전전항 + 전항) % 10`을 저장했는지, 총 개수가 정확히 10개인지 확인.

```runner
@@SOLUTION
a, b = map(int, input().split())
arr = [a, b]
for i in range(2, 10):
    arr.append((arr[i - 1] + arr[i - 2]) % 10)
for x in arr:
    print(x, end=" ")
print()
@@TESTS
--IN
2 5
--OUT
2 5 7 2 9 1 0 1 1 2
--IN
7 3
--OUT
7 3 0 3 3 6 9 5 4 9
@@EXPL
접근 방법: 처음 두 항을 리스트에 넣고, 세 번째 항부터는 바로 앞 두 항의 합을 10으로 나눈 나머지를 계속 `append`해 총 10개를 채운 뒤 공백 구분으로 출력합니다.

코드 진행 과정:

- `a, b = map(int, input().split())`로 첫째·둘째 항을 정수로 받아 `arr = [a, b]`로 시작합니다(길이 2).
- `for i in range(2, 10)`은 인덱스 2부터 9까지, 즉 8번 반복해 리스트를 총 10개로 만듭니다.
- 각 단계에서 `(arr[i-1] + arr[i-2]) % 10` — 바로 앞 항과 전전 항의 합의 일의 자리 — 를 `append`합니다. 예: 인덱스 2는 (5+2)%10=7, 인덱스 3은 (7+5)%10=2.
- 완성된 리스트를 `for x in arr: print(x, end=" ")`로 공백 구분해 한 줄에 출력합니다.

다시 작성할 때 생각 순서:

- 초기 두 항을 리스트에 담아 시작한다.
- 몇 개를 더 채워야 하는지 세어(10 - 2 = 8) 반복 범위를 정한다.
- 새 항은 "직전 두 항의 합의 일의 자리"이므로 `arr[i-1]`, `arr[i-2]`를 더하고 `% 10`을 적용해 `append`한다.
- 마지막에 10개를 공백 구분으로 출력한다.
```

## ✅ 마스터 체크리스트

- [ ] 리스트를 `[]`로 만들고 인덱스(0부터, 음수 포함)로 요소를 읽고 수정할 수 있다
- [ ] `append`·`insert`·`remove`·`pop`·`sort`·`reverse`·`count`·`index`·`len`의 동작을 구분해 쓸 수 있다
- [ ] `input().split()`과 `list(map(int, input().split()))`로 여러 값을 리스트로 입력받을 수 있다
- [ ] `for i in range(len(arr))`와 `for x in arr`의 차이를 알고 상황에 맞게 선택한다
- [ ] 리스트 컴프리헨션 `[표현식 for 변수 in 시퀀스]`로 배열을 변환할 수 있다
- [ ] 결과 리스트를 공백 구분 또는 이어 붙이기 등 요구된 형식으로 출력할 수 있다
- [ ] 나머지 연산(`% 10`)과 이전 항 참조로 수열형 배열을 채울 수 있다

## ⚠️ 자주 하는 실수

- 인덱스를 1부터로 착각 — 리스트 인덱스는 0부터 시작하며, "N번 인덱스"와 "N번째"는 다릅니다.
- 범위 초과 인덱스 접근(`IndexError`), 없는 요소 `remove`/`index`(`ValueError`)를 유발.
- 입력을 `int`로 변환하지 않아 문자열 리스트로 계산해 오류/오답 발생.
- `+`/`*`는 새 리스트 반환(원본 유지), `sort()`/`reverse()`는 원본 변경 — 반환값 유무를 혼동.
- 출력 형식 실수 — 공백 구분해야 하는데 붙여 쓰거나, 붙여야 하는데 공백을 넣음(`print(*arr)` vs `"".join(...)` 구분).
- 수열형 문제에서 "합 자체"를 저장하고 일의 자리(`% 10`)를 빠뜨림.
