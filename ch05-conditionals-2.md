# Ch5. 조건문 2 — Codetree 101 스터디 가이드

> 언어 Python3 · 2개 레슨 · 개념 2 / 문제 6

## 📌 챕터 개요

여러 조건을 한 번에 다루는 방법을 배웁니다. `and`/`or` 논리 연산자로 두 개 이상의 조건을 하나의 복합 조건으로 묶는 법과, `if` 안에 다시 `if`를 넣어 조건을 단계별로 세분화하는 중첩 조건문을 익힙니다. 학습 목표는 성인 여부·윤년·중앙값처럼 조건이 얽힌 실제 문제를 논리적으로 분해하여 정확한 분기 코드로 옮기는 능력을 기르는 것입니다.

## 📖 개념 정리 (레슨별)

### L1. 논리 연산자를 이용한 조건문

**핵심 개념**
- 조건이 2개 이상일 때 `and`와 `or`로 여러 조건을 하나의 복합 조건으로 결합합니다.
- `and`는 **모든** 조건이 참이어야 전체가 참, `or`는 **하나 이상**의 조건이 참이면 전체가 참입니다.
- 기본 조건은 `and`로 묶고, "예외적으로 통과시키는" 조건은 `or`로 덧붙이는 식으로 논리를 조립하면 복잡한 규칙도 단계적으로 표현할 수 있습니다.

**문법/사용법**
- `조건1 and 조건2` : 둘 다 참일 때만 참
- `조건1 or 조건2` : 둘 중 하나라도 참이면 참
- 우선순위가 헷갈리면 각 조건을 괄호로 묶어 `(A and B) or C` 처럼 의도를 명확히 합니다.

**예시**
```python
# 성인 판단: (나이 18 이상 그리고 시민권 보유) 또는 특별 허가
age = 20
has_citizenship = True
has_special_permission = False

if (age >= 18 and has_citizenship) or has_special_permission:
    print("성인입니다.")
else:
    print("미성년자입니다.")

# 윤년 판단: (4의 배수 그리고 100의 배수 아님) 또는 400의 배수
year = 2000
if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print("윤년")
```

**주의·흔한 실수**
- `and`/`or`의 결합 순서를 착각해 괄호를 빠뜨리면 의도와 다른 조건이 됩니다. 예외 조건(`or`)은 반드시 괄호로 그룹을 구분하세요.
- "18 이상"과 "18 초과"처럼 경계값 포함 여부(`>=` vs `>`)를 문제 문장에서 정확히 읽어야 합니다.
- 배수 판정은 `x % k == 0` 형태를 씁니다. "나누어떨어지지 않음"은 `x % k != 0` 입니다.

### L2. 중첩 조건문

**핵심 개념**
- 중첩 `if`는 `if` 블록 안에 또 다른 `if`를 두는 구조로, 큰 조건을 먼저 확인한 뒤 참일 때만 하위 조건을 세분화합니다.
- 복잡한 규칙을 "1단계 → 2단계"로 나누어 검사하므로 논리 흐름이 명확해집니다. (예: 여권이 유효한가? → 유효하면 비자가 필요한가?)

**문법/사용법**
```python
if 조건1:
    if 조건2:
        # 조건1, 조건2 모두 참
    else:
        # 조건1 참, 조건2 거짓
else:
    # 조건1 거짓
```
- 들여쓰기 깊이로 어떤 `else`가 어떤 `if`에 속하는지 결정되므로 들여쓰기를 정확히 맞춰야 합니다.

**예시**
```python
passport_valid = True
visa_required = False

if passport_valid:
    if visa_required:
        print("비자를 발급받습니다.")
    else:
        print("바로 여행을 떠납니다.")
else:
    print("여권을 새로 발급받습니다.")
```

**주의·흔한 실수**
- `else`가 짝이 되는 `if`는 들여쓰기 레벨로 결정됩니다. 안쪽 `else`인지 바깥 `else`인지 혼동하기 쉽습니다.
- 많은 경우 동일한 분기를 `and`/`or` 복합 조건이나 `elif` 로도 표현할 수 있습니다. 어느 쪽이 더 읽기 쉬운지 고민해보세요.
- 중첩이 3단계 이상 깊어지면 가독성이 떨어지므로 조건을 논리 연산자로 합칠 수 있는지 점검합니다.

## 🧩 문제 채점용 정리

### 1) 점수 비교 (Easy · 10XP · 정답률 82%)
- **요구사항**: A와 B의 수학·영어 점수를 받아 A가 B보다 두 과목 모두 점수가 높은지 판정.
- **입력**: 1번째 줄에 A의 수학·영어 점수(공백 구분), 2번째 줄에 B의 수학·영어 점수(공백 구분).
- **출력**: A의 수학·영어 점수가 **둘 다** B보다 크면 `1`, 아니면 `0`.
- **예제**: `100 85 / 98 78` → `1` · `86 93 / 86 90` → `0`
- **제한**: Time 1000 ms · Memory 80 MiB · 점수는 1~100.
- **연습 개념**: `and` 로 두 비교 조건을 결합.
- **셀프체크 포인트**: 두 번째 예제(수학이 같음)에서 `>=`가 아니라 `>`(초과)로 비교했는지 확인.

```runner
@@SOLUTION
am, ae = map(int, input().split())
bm, be = map(int, input().split())
if am > bm and ae > be:
    print(1)
else:
    print(0)
@@TESTS
--IN
100 85
98 78
--OUT
1
--IN
86 93
86 90
--OUT
0
--IN
90 90
80 80
--OUT
1
@@EXPL
**접근 방법**: 두 과목 모두 A가 더 높아야 하므로 두 비교 조건을 `and`로 묶습니다. "둘 다"라는 표현이 나오면 `and`를 떠올리세요.

**코드 진행 과정**

- 첫 줄에서 A의 수학·영어 점수를, 둘째 줄에서 B의 점수를 `split()`으로 나눠 정수로 받습니다.
- `am > bm and ae > be` 를 검사합니다. 두 조건이 모두 참일 때만 전체가 참이 됩니다.
- 참이면 `1`, 아니면 `0`을 출력합니다.

**스스로 다시 짤 때**

- "두 과목 모두 초과"라는 조건을 그대로 `>`(초과) 두 개로 옮기고 `and`로 연결한다고 생각하세요.
- 경계값 주의: 점수가 같은 경우(`==`)는 "더 높다"가 아니므로 `>=`가 아니라 `>`를 써야 합니다. 두 번째 예제가 이걸 검증합니다.
```

### 2) 정수의 조건 여부 3 (Easy · 10XP · 정답률 90%)
- **요구사항**: 정수 a가 13의 배수이거나 19의 배수이면 참, 아니면 거짓 판정.
- **입력**: 1번째 줄에 정수 a.
- **출력**: 조건 만족 시 `True`, 아니면 `False` (파이썬 불리언 표기).
- **예제**: `361` → `True` (19×19) · `100` → `False`
- **제한**: Time 1000 ms · Memory 64 MiB · 1≤a≤1,000.
- **연습 개념**: `or` 로 두 배수 조건을 결합, `a % 13 == 0`.
- **셀프체크 포인트**: 출력이 `true`가 아닌 `True`(첫 글자 대문자, 파이썬 불리언). 비교식 결과를 그대로 `print` 하면 자연스럽게 맞습니다.

```runner
@@SOLUTION
a = int(input())
print(a % 13 == 0 or a % 19 == 0)
@@TESTS
--IN
361
--OUT
True
--IN
100
--OUT
False
--IN
26
--OUT
True
@@EXPL
**접근 방법**: "13의 배수 이거나 19의 배수"는 두 조건 중 하나라도 참이면 되므로 `or`로 묶습니다. 배수 판정은 나머지가 0인지(`% == 0`)로 합니다.

**코드 진행 과정**

- 정수 `a`를 입력받습니다.
- `a % 13 == 0` (13의 배수)와 `a % 19 == 0` (19의 배수)를 `or`로 연결합니다.
- 이 비교식 자체가 파이썬 불리언(`True`/`False`)이므로, 그대로 `print`에 넣으면 대문자 표기 불리언이 출력됩니다.

**스스로 다시 짤 때**

- "이거나"라는 말이 나오면 `or`를 떠올리세요.
- 출력이 소문자 `true`가 아니라 대문자 `True`임에 주의합니다. 비교식 결과를 별도 문자열로 바꾸지 말고 그대로 출력하면 자연스럽게 `True`/`False`가 됩니다.
```

### 3) 굉장한 수 (Easy · 10XP · 정답률 83%)
- **요구사항**: 자연수 N이 "홀수이면서 3의 배수" 또는 "짝수이면서 5의 배수"이면 굉장한 수.
- **입력**: 1번째 줄에 N.
- **출력**: 굉장한 수면 `true`, 아니면 `false` (소문자 문자열).
- **예제**: `9` → `true` (홀수∧3배수) · `18` → `false` (짝수지만 5배수 아님)
- **제한**: Time 1000 ms · Memory 80 MiB · 1≤N≤100.
- **연습 개념**: `(홀수 and 3배수) or (짝수 and 5배수)` — `and`/`or` 혼합.
- **셀프체크 포인트**: 출력이 소문자 `true`/`false` 문자열. 앞 문제의 `True`와 표기가 다르니 문자열로 직접 출력해야 합니다.

```runner
@@SOLUTION
n = int(input())
if (n % 2 == 1 and n % 3 == 0) or (n % 2 == 0 and n % 5 == 0):
    print("true")
else:
    print("false")
@@TESTS
--IN
9
--OUT
true
--IN
18
--OUT
false
--IN
10
--OUT
true
@@EXPL
**접근 방법**: 두 개의 묶음 조건 "홀수 그리고 3의 배수", "짝수 그리고 5의 배수" 를 각각 `and`로 만들고, 이 둘을 `or`로 잇습니다. `and`/`or`가 섞이므로 각 묶음을 괄호로 그룹화합니다.

**코드 진행 과정**

- `n`을 입력받습니다.
- 홀수 여부는 `n % 2 == 1`, 짝수는 `n % 2 == 0`으로 판정합니다.
- `(홀수 and 3배수) or (짝수 and 5배수)` 형태로 조건을 조립합니다.
- 참이면 소문자 문자열 `"true"`, 아니면 `"false"`를 직접 출력합니다.

**스스로 다시 짤 때**

- "A이면서 B" 는 `and`, 두 경우를 "또는" 으로 합치는 것은 `or` 라고 대응시키세요.
- 괄호로 `(A and B)` 각 묶음을 명확히 감싸 우선순위 혼동을 막습니다.
- 이 문제는 불리언이 아니라 소문자 문자열 출력이므로, 조건 결과를 `print(...)`로 바로 내보내지 말고 `"true"`/`"false"` 문자열을 분기해서 출력해야 합니다.
```

### 4) 남녀노소 구분짓기 (Easy · 10XP · 정답률 80%)
- **요구사항**: 성별(남=0, 여=1)과 나이를 받아 성인/미성년 × 남/여 4분류. 19세 이상이 성인.
- **입력**: 1번째 줄에 성별 숫자(0 또는 1), 2번째 줄에 나이(자연수).
- **출력**: `MAN`(성인남), `WOMAN`(성인여), `BOY`(미성년남), `GIRL`(미성년여) 중 하나.
- **예제**: `0 / 23` → `MAN` · `1 / 13` → `GIRL`
- **제한**: Time 1000 ms · Memory 80 MiB · 1≤나이≤100.
- **연습 개념**: 중첩 `if`(먼저 성별 분기 → 안에서 나이 분기) 또는 `and` 복합 조건.
- **셀프체크 포인트**: 성인 기준이 "19세 이상"이므로 `age >= 19`. 4가지 조합이 빠짐없이 처리되는지 확인.

```runner
@@SOLUTION
gender = int(input())
age = int(input())
if gender == 0:
    if age >= 19:
        print("MAN")
    else:
        print("BOY")
else:
    if age >= 19:
        print("WOMAN")
    else:
        print("GIRL")
@@TESTS
--IN
0
23
--OUT
MAN
--IN
1
13
--OUT
GIRL
--IN
0
10
--OUT
BOY
@@EXPL
**접근 방법**: 성별(2가지) × 성인 여부(2가지) = 4분류이므로, 바깥 `if`로 성별을 먼저 나누고 그 안에서 다시 나이로 나누는 중첩 조건문을 씁니다.

**코드 진행 과정**

- 첫 줄에서 성별(0=남, 1=여), 둘째 줄에서 나이를 입력받습니다.
- 바깥 `if gender == 0:` 로 남/여를 분기합니다.
- 각 분기 안쪽에서 `if age >= 19:` 로 성인/미성년을 다시 나눠 4가지 중 하나를 출력합니다.

**스스로 다시 짤 때**

- 먼저 큰 축(성별)으로 두 갈래, 그 안에서 작은 축(나이)으로 다시 두 갈래를 그린다고 생각하세요. 표를 그리면 4칸이 나옵니다: MAN/BOY/WOMAN/GIRL.
- 성인 기준이 "19세 이상"이므로 `>= 19`(이상)로 씁니다. `> 19`로 쓰면 19세가 미성년으로 잘못 분류됩니다.
- 안쪽 `else`가 안쪽 `if`에 짝이 되도록 들여쓰기 레벨을 정확히 맞춥니다.
```

### 5) 윤년인가 (Easy · 10XP · 정답률 60%)
- **요구사항**: 연도 Y가 윤년인지 판정. 4의 배수는 윤년, 단 100의 배수이면서 400의 배수가 아니면 평년.
- **입력**: 1번째 줄에 자연수 Y.
- **출력**: 윤년이면 `true`, 아니면 `false`.
- **예제**: `2020` → `true` · `2200` → `false` (100의 배수인데 400의 배수 아님)
- **제한**: Time 1000 ms · Memory 80 MiB · 1≤Y≤2222.
- **연습 개념**: `(Y%4==0 and Y%100!=0) or (Y%400==0)` — 논리 연산자 결합 또는 중첩 조건.
- **셀프체크 포인트**: 400의 배수 예외(예: 2000)를 놓치지 않았는지, 괄호로 `and`/`or` 그룹을 정확히 묶었는지 확인.

```runner
@@SOLUTION
y = int(input())
if (y % 4 == 0 and y % 100 != 0) or (y % 400 == 0):
    print("true")
else:
    print("false")
@@TESTS
--IN
2020
--OUT
true
--IN
2200
--OUT
false
--IN
2000
--OUT
true
@@EXPL
**접근 방법**: 윤년 규칙은 "4의 배수이면서 100의 배수는 아님" 또는 "400의 배수" 입니다. 앞 묶음은 `and`, 두 경우를 잇는 것은 `or`로 표현합니다.

**코드 진행 과정**

- 연도 `y`를 입력받습니다.
- `y % 4 == 0 and y % 100 != 0` (4의 배수지만 100의 배수는 아님)을 한 묶음으로 만듭니다.
- 여기에 `or y % 400 == 0` (400의 배수 예외)을 덧붙입니다.
- 참이면 `"true"`, 아니면 `"false"`를 출력합니다.

**스스로 다시 짤 때**

- "4의 배수" 를 기본으로 하되 "100의 배수는 평년" 예외를 `and ... != 0`로 걸러내고, "400의 배수는 다시 윤년" 예외를 `or`로 되살린다고 순서대로 생각하세요.
- 2000처럼 400의 배수인 경우가 `or` 덕분에 `true`로 살아나는지 꼭 확인합니다(2200은 100의 배수지만 400의 배수가 아니어서 `false`).
- 각 묶음을 괄호로 감싸 `and`/`or` 우선순위 혼동을 막습니다.
```

### 6) 중앙값 구하기 (Easy · 10XP · 정답률 64%)
- **요구사항**: 서로 다른 세 정수 A, B, C 중 오름차순 정렬 시 가운데 값(중앙값)을 구하기.
- **입력**: 1번째 줄에 A, B, C(공백 구분).
- **출력**: 세 값 중 중앙값.
- **예제**: `26 22 30` → `26` · `-20 0 23` → `0`
- **제한**: Time 1000 ms · Memory 80 MiB · −100≤A,B,C≤100, 세 값은 서로 다름.
- **연습 개념**: 비교 조건 조합 / 중첩 조건으로 최대·최소를 제외한 값 찾기(정렬 활용도 가능).
- **셀프체크 포인트**: 음수 예제와 순서가 뒤섞인 입력에서도 맞는지, "최댓값도 최솟값도 아닌 값"을 정확히 골랐는지 확인.

```runner
@@SOLUTION
a, b, c = map(int, input().split())
if (a > b and a < c) or (a > c and a < b):
    print(a)
else:
    if (b > a and b < c) or (b > c and b < a):
        print(b)
    else:
        print(c)
@@TESTS
--IN
26 22 30
--OUT
26
--IN
-20 0 23
--OUT
0
--IN
5 1 3
--OUT
3
@@EXPL
**접근 방법**: 중앙값은 "하나보다는 크고 다른 하나보다는 작은" 값입니다. 세 값이 서로 다르므로, 각 값이 이 조건을 만족하는지 비교로 확인합니다.

**코드 진행 과정**

- `a, b, c`를 공백으로 나눠 정수로 받습니다.
- `a`가 중앙값인지 검사합니다: `(a > b and a < c)` (b보다 크고 c보다 작음) 또는 순서를 뒤집은 `(a > c and a < b)`. 둘 중 하나면 `a`가 가운데입니다.
- 아니라면 같은 방식으로 `b`가 중앙값인지 검사합니다.
- 둘 다 아니면 남은 값 `c`가 중앙값이므로 출력합니다.

**스스로 다시 짤 때**

- "중앙값 = 최대도 최소도 아닌 값" 을 "한 값보다 크고 다른 한 값보다 작은 값" 으로 바꿔 생각하세요.
- 한 값(예: a)에 대해 나머지 둘과의 대소 순서는 두 가지(`b<a<c` 또는 `c<a<b`)뿐이므로 `or`로 두 경우를 묶습니다.
- 세 값이 서로 다르다는 조건 덕분에 등호(`=`)를 신경 쓰지 않아도 됩니다. a도 b도 아니면 자동으로 c가 답이라는 점을 이용해 마지막 분기를 단순화합니다.
```

## ✅ 마스터 체크리스트

- [ ] `and`는 모든 조건, `or`는 하나 이상의 조건이 참일 때 참임을 설명할 수 있다.
- [ ] `(A and B) or C` 형태로 기본 조건과 예외 조건을 괄호로 묶어 조립할 수 있다.
- [ ] 배수/나머지 판정을 `% == 0`, `% != 0`로 정확히 쓸 수 있다.
- [ ] 경계값 포함 여부(`>=` vs `>`, "이상"/"초과")를 문제에서 정확히 읽고 반영한다.
- [ ] 중첩 `if`의 구조와 `else`가 짝이 되는 `if`를 들여쓰기로 구분할 수 있다.
- [ ] 윤년 규칙(4·100·400)을 논리 연산자로 정확히 구현할 수 있다.
- [ ] 세 값의 중앙값을 최대·최소 제외 논리로 구할 수 있다.
- [ ] 출력이 `True`/`False`(불리언)인지 `true`/`false`(문자열)인지 문제별로 구분한다.

## ⚠️ 자주 하는 실수

- **괄호 누락**: `A and B or C`를 의도와 다르게 해석. 예외 조건은 `(A and B) or C`처럼 괄호로 그룹을 명시.
- **경계값 오류**: "19세 이상 성인"을 `> 19`로 잘못 쓰는 등 `>=`/`>` 혼동.
- **출력 표기 불일치**: "정수의 조건 여부 3"은 `True`/`False`(불리언), "굉장한 수"·"윤년인가"는 `true`/`false`(소문자 문자열). 문제 지문을 그대로 따를 것.
- **윤년 400 예외 누락**: 100의 배수만 걸러내고 400의 배수 예외를 빠뜨려 2000년 같은 케이스를 틀림.
- **중첩 else 오귀속**: 들여쓰기를 잘못 맞춰 안쪽 `if`의 `else`가 바깥 `if`에 붙는 실수.
- **분류 누락**: 남녀노소·중앙값처럼 경우의 수가 많은 문제에서 일부 조합을 처리하지 않아 특정 입력에서만 오답.
