# Ch3. 조건문 1 — Codetree 101 스터디 가이드

> 언어 Python3 · 4개 레슨 · 개념 4 / 문제 8

## 📌 챕터 개요

프로그램이 상황에 따라 다른 동작을 하도록 만드는 조건문을 배웁니다. 참/거짓으로 평가되는 조건식을 세우고, `if` / `if-else` / `if-elif-else` 구조로 코드 흐름을 분기하는 법을 익힙니다. 비교 연산자와 들여쓰기 규칙을 정확히 이해해 단일 조건부터 다중 조건 분기까지 안정적으로 처리하는 것이 목표입니다.

## 📖 개념 정리 (레슨별)

### L1. 조건문 입문

**핵심 개념**
- 조건문은 "특정 조건이 참일 때만 특정 코드를 실행"하도록 하는 구조입니다. 덕분에 상황마다 다른 동작을 하는 유연한 프로그램을 만들 수 있습니다.
- 조건문은 크게 두 부분으로 나뉩니다: (1) 참(True)/거짓(False)으로 평가되는 **조건**, (2) 조건이 참일 때 수행할 **동작**.
- 예: "온도가 30도 이상이면 에어컨을 켠다" → 조건은 "온도 ≥ 30", 동작은 "에어컨 켜기".

**문법/사용법**
- 이 레슨은 개념 소개 단계로, 구체적 문법보다 "언제·왜 조건문이 필요한가"에 초점을 둡니다.
- 다양한 상황 처리, 유연성 향상, 불필요한 실행 방지(효율성)라는 세 가지 이유로 조건문이 쓰입니다.

**예시**
```python
# 아이디어: 온도가 30 이상이면 에어컨을 켠다
temp = 32
if temp >= 30:
    print("에어컨을 켠다")
```

**주의·흔한 실수**
- 조건은 반드시 True/False로 판정 가능한 표현식이어야 합니다. 단순 값(예: 그냥 숫자)만으로는 "무엇을 판별하는지" 명확히 설계해야 합니다.
- 예시 문제(짝수/홀수 판별)에서는 나머지 연산 `n % 2 == 0` 같은 참/거짓 조건을 세우는 감각을 먼저 익히세요.

### L2. If

**핵심 개념**
- `if 문`은 하나의 조건을 평가하여 **참일 때만** 해당 코드 블록을 실행합니다. 거짓이면 그 블록을 건너뜁니다.
- 조건에는 비교 연산자로 만든 논리 표현식을 사용합니다: `==`, `!=`, `<`, `>`, `<=`, `>=`.

**문법/사용법**
```
if 조건:
    실행할 동작
```
- `if 조건:` 다음 줄부터는 반드시 **들여쓰기**로 실행 블록을 표시합니다. 파이썬은 들여쓰기로 코드 블록을 구분합니다.
- 조건이 거짓이면 들여쓴 블록을 통째로 건너뛰고, 들여쓰지 않은 다음 코드로 넘어갑니다.

**예시**
```python
age = int(input())

if age >= 18:
    print("성인입니다")
    print("good")

print("확인 완료되었습니다")
# age=20 → 성인입니다 / good / 확인 완료되었습니다
# age=10 → 확인 완료되었습니다  (if 블록은 건너뜀)
```

**주의·흔한 실수**
- `if` 다음 줄을 들여쓰지 않으면 `IndentationError: expected an indented block after 'if' statement`가 발생합니다.
- 어디까지가 조건부 블록이고 어디부터가 조건과 무관한 코드인지, 들여쓰기 수준으로 명확히 구분하세요.
- 같음 비교는 `==`이며 대입 연산자 `=`와 혼동하지 않도록 주의합니다.

### L3. Else

**핵심 개념**
- `if-else`는 "조건이 참이면 A, 거짓이면 B"처럼 **두 갈래 중 하나**를 반드시 실행하는 구조입니다.
- 조건이 True면 `if` 블록, False면 `else` 블록이 실행됩니다.

**문법/사용법**
```
if 조건:
    조건이 True일 때 실행할 코드
else:
    조건이 False일 때 실행할 코드
```
- `else`에는 조건을 붙이지 않습니다(그냥 `else:`). "그 외 모든 경우"를 담당합니다.
- `else`는 선택 사항입니다. 참일 때만 처리하면 되는 경우 `if`만 써도 됩니다.

**예시**
```python
weather = "맑음"
if weather == "비":
    print("우산을 챙긴다")
else:
    print("모자를 쓴다")
# 출력: 모자를 쓴다
```

**주의·흔한 실수**
- `if`와 `else`는 같은 들여쓰기 수준에 있어야 하고, 각 블록 내부는 한 단계 더 들여씁니다.
- `else:` 뒤에 조건을 적지 않도록 주의하세요(조건이 필요하면 `elif`를 씁니다).
- 참일 때와 거짓일 때 동작을 모두 정의하고 싶다면 `else`를 함께 써서 흐름을 명확히 만드세요.

### L4. Else If

**핵심 개념**
- `if-elif-else`는 여러 조건을 **위에서 아래로 순차 검사**하여, 처음으로 참이 된 블록 하나만 실행하는 다중 분기 구조입니다.
- 위쪽 `if`가 참이면 그 블록만 실행하고 나머지는 건너뜁니다. 거짓이면 다음 `elif`를 확인하며, 모두 거짓이면 `else`가 실행됩니다.

**문법/사용법**
```
if 조건1:
    조건1이 True일 때 실행할 코드
elif 조건2:
    조건1이 False이고 조건2가 True일 때 실행할 코드
elif 조건3:
    앞 조건이 모두 False이고 조건3이 True일 때 실행할 코드
else:
    모든 조건이 False일 때 실행할 코드
```
- `elif`와 `else`는 선택적입니다. `if`+`elif`만, 또는 `if`+`else`만 쓰는 형태도 가능합니다.

**예시**
```python
weather = "눈"
if weather == "맑음":
    activity = "공원 산책"
elif weather == "비":
    activity = "집에서 영화 보기"
elif weather == "눈":
    activity = "스키 타기"
else:
    activity = "카페에서 책 읽기"
print(f"오늘의 활동은 {activity}입니다.")
# 출력: 오늘의 활동은 스키 타기입니다.
```

**주의·흔한 실수**
- 조건은 순서대로 평가되므로 **범위가 좁은(엄격한) 조건을 위에, 넓은 조건을 아래에** 두어야 합니다. 예: 등급 판정에서 90 이상을 60 이상보다 먼저 검사해야 합니다.
- 한 번 참이 되면 이후 `elif`/`else`는 검사하지 않습니다. 여러 `if`를 나열하는 것과 동작이 다릅니다.
- 경계값(예: `>=` vs `>`)을 문제 조건에 맞게 정확히 설정하세요.

## 🧩 문제 채점용 정리

### 1) 음수 구별하기 (Easy · 10XP · 정답률 79%)
- **요구사항**: 정수 하나를 입력받아 그대로 출력하고, 그 값이 음수이면 추가로 표시합니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: 첫째 줄에 입력받은 정수를 출력. 그 정수가 음수이면 둘째 줄에 `minus`를 추가 출력.
- **예제**: `-13` → `-13` / `minus`  |  `15` → `15`
- **제한**: −100 ≤ N ≤ 100 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: `if`(단일 조건)
- **셀프체크 포인트**: 음수 판정 조건(`N < 0`)에서 0을 음수로 처리하지 않는지 확인. 양수/0일 때는 둘째 줄을 아예 출력하지 않아야 함.

```runner
@@SOLUTION
n = int(input())
print(n)
if n < 0:
    print("minus")
@@TESTS
--IN
-13
--OUT
-13
minus
--IN
15
--OUT
15
--IN
0
--OUT
0
@@EXPL
접근: 값을 먼저 그대로 출력한 뒤, "음수일 때만" 한 줄을 더 붙이는 문제이므로 `else`가 필요 없는 단일 `if` 구조로 풉니다.

코드 진행:

- `n = int(input())`로 정수 하나를 읽습니다.
- `print(n)`으로 입력값을 무조건 먼저 출력합니다.
- `if n < 0:`이 참일 때만 `print("minus")`를 실행합니다. 0이나 양수면 이 블록을 건너뜁니다.

다시 짤 때 생각 순서:

- "항상 하는 일(값 출력)"과 "조건부로 하는 일(minus 출력)"을 구분합니다.
- 음수 기준을 `< 0`으로 잡아 0이 걸리지 않게 합니다(0은 음수가 아님).
- 조건부 출력은 `else` 없이 `if` 하나로 충분하다는 점을 떠올립니다.
```

### 2) 비교 연산 (Easy · 10XP · 정답률 84%)
- **요구사항**: 두 정수 A, B에 대해 6가지 비교 결과를 각각 참(1)/거짓(0)으로 판정해 순서대로 출력합니다.
- **입력**: 첫 줄에 정수 A, B가 공백으로 구분되어 주어짐.
- **출력**: 6줄에 걸쳐 1/0 출력 — ① A≥B ② A>B ③ B≥A ④ B>A ⑤ A==B ⑥ A!=B
- **예제**: `4 3` → `1 / 1 / 0 / 0 / 0 / 1`  |  `3 3` → `1 / 0 / 1 / 0 / 1 / 0`
- **제한**: 1 ≤ A,B ≤ 100 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: 비교 연산자, 조건식 결과
- **셀프체크 포인트**: 각 비교 연산자(`>=`, `>`, `==`, `!=`)를 정확히 매칭했는지, 출력 순서가 문제에 명시된 6개 순서와 일치하는지 확인. 불리언을 정수 1/0으로 변환하는 방법을 떠올려 보세요.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
print(int(a >= b))
print(int(a > b))
print(int(b >= a))
print(int(b > a))
print(int(a == b))
print(int(a != b))
@@TESTS
--IN
4 3
--OUT
1
1
0
0
0
1
--IN
3 3
--OUT
1
0
1
0
1
0
--IN
2 5
--OUT
0
0
1
1
0
1
@@EXPL
접근: 여섯 가지 비교식의 참/거짓을 그대로 1/0으로 바꿔 순서대로 출력하는 문제입니다. `if` 분기 없이 비교식 결과를 정수로 변환해 출력하면 간단합니다.

코드 진행:

- `input().split()`으로 한 줄에 들어온 두 값을 나눠 각각 정수로 바꿉니다.
- 각 비교식(`a >= b`, `a > b`, `b >= a`, `b > a`, `a == b`, `a != b`)은 True/False를 만듭니다.
- 파이썬에서 True는 1, False는 0이므로 `int(...)`로 감싸 1/0으로 출력합니다.
- 문제에 명시된 여섯 개 순서를 그대로 지켜 한 줄씩 출력합니다.

다시 짤 때 생각 순서:

- 먼저 두 정수를 읽는 코드를 만듭니다.
- 요구된 여섯 비교를 순서표대로 나열합니다.
- 불리언을 1/0으로 바꾸려면 `int()`로 감싸면 된다는 점을 기억합니다.
```

### 3) 시험 통과 여부 확인하기 (Easy · 10XP · 정답률 73%)
- **요구사항**: 점수를 입력받아 통과 여부를 판정하고, 미통과 시 통과까지 부족한 점수를 안내합니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: N이 80 이상이면 `pass`. 그렇지 않으면 부족한 점수를 계산해 `x more score` 형식으로 출력.
- **예제**: `87` → `pass`  |  `57` → `23 more score`
- **제한**: 0 ≤ N ≤ 100 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: `if-else`
- **셀프체크 포인트**: 기준값 80은 통과(경계 포함, `>= 80`)임에 유의. 부족 점수는 `80 - N`. 출력 문자열에서 숫자와 `more score` 사이 공백/형식을 정확히 맞추세요.

```runner
@@SOLUTION
n = int(input())
if n >= 80:
    print("pass")
else:
    print(str(80 - n) + " more score")
@@TESTS
--IN
87
--OUT
pass
--IN
57
--OUT
23 more score
--IN
80
--OUT
pass
@@EXPL
**이 풀이의 새 문법**

- `str(값)`: 정수·실수를 문자열로 바꾼다. 문자열과 `+`로 이어 붙이려면 숫자도 먼저 문자열로 변환해야 한다.

접근: "통과냐 아니냐" 두 갈래로 나뉘므로 `if-else` 구조가 딱 맞습니다. 통과 기준(80 이상)을 경계 포함으로 잡는 것이 핵심입니다.

코드 진행:

- `n`을 정수로 읽습니다.
- `if n >= 80:`이면 `pass`를 출력합니다. 80은 통과이므로 `>=`를 씁니다.
- `else`(80 미만)에서는 부족 점수 `80 - n`을 계산해 `숫자 more score` 형식으로 출력합니다.
- 숫자를 문자열과 이어 붙이기 위해 `str(80 - n)`으로 바꾼 뒤 `+ " more score"`로 연결합니다.

다시 짤 때 생각 순서:

- 두 경우(통과/미통과)를 먼저 나눕니다.
- 경계값 80이 어느 쪽인지 결정해 `>=`인지 `>`인지 정합니다(문제상 80은 통과).
- 미통과 출력은 "부족 점수 + 고정 문구"이므로, 계산 결과를 문자열로 만들어 이어 붙입니다.
```

### 4) 비교에 따른 연산 (Easy · 10XP · 정답률 78%)
- **요구사항**: 두 정수 a, b 중 a가 b보다 크면 두 수의 곱을, 아니면 b를 a로 나눈 몫을 출력합니다.
- **입력**: 첫 줄에 정수 a, b가 공백으로 구분되어 주어짐.
- **출력**: a>b이면 `a*b`, 그렇지 않으면 `b // a`(몫)를 출력.
- **예제**: `10 5` → `50`  |  `3 10` → `3`
- **제한**: 1 ≤ a,b ≤ 100 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: `if-else`, 나눗셈 몫 연산
- **셀프체크 포인트**: "곱"과 "나눈 몫"의 방향(b ÷ a)에 주의. 몫은 정수 나눗셈(`//`)을 사용. 두 번째 예제에서 10 // 3 = 3이 맞는지 검산해 보세요.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
if a > b:
    print(a * b)
else:
    print(b // a)
@@TESTS
--IN
10 5
--OUT
50
--IN
3 10
--OUT
3
--IN
4 4
--OUT
1
@@EXPL
접근: a와 b의 크기 관계에 따라 서로 다른 계산을 하는 문제이므로 `if-else`로 두 경우를 나눕니다. 나눗셈 방향(b를 a로 나눔)과 몫 연산자를 정확히 쓰는 것이 관건입니다.

코드 진행:

- 두 값을 읽어 각각 정수 `a`, `b`로 만듭니다.
- `if a > b:`이면 두 수의 곱 `a * b`를 출력합니다.
- 그렇지 않으면(a가 b보다 크지 않으면) `else`에서 `b // a`를 출력합니다. `//`는 정수 나눗셈의 몫입니다.
- 예: `3 10`은 a>b가 거짓이므로 `10 // 3 = 3`. `4 4`도 a>b가 거짓이라 `4 // 4 = 1`.

다시 짤 때 생각 순서:

- 조건은 "a가 b보다 큰가?" 하나뿐이므로 `if-else` 한 쌍으로 충분합니다.
- 참일 때 곱, 거짓일 때 몫이라는 대응을 명확히 적어 둡니다.
- 몫은 반드시 `//`를 쓰고, 나누는 방향이 `b // a`임을 문제에서 다시 확인합니다.
```

### 5) 특정 조건 두 정수 비교 (Easy · 10XP · 정답률 78%)
- **요구사항**: 두 정수 A, B에 대해 두 판정 결과를 한 줄에 공백으로 구분해 출력합니다.
- **입력**: 첫 줄에 정수 A, B가 공백으로 구분되어 주어짐.
- **출력**: (첫째 값) A<B이면 1, 아니면 0 / (둘째 값) A==B이면 1, 아니면 0 — 두 값을 공백으로 구분해 한 줄에 출력.
- **예제**: `8 10` → `1 0`  |  `15 15` → `0 1`
- **제한**: 1 ≤ A,B ≤ 100 · Time 1000 ms · Memory 64 MiB
- **연습 개념**: 비교 연산, 한 줄 출력 형식
- **셀프체크 포인트**: 두 결과를 세로가 아닌 **한 줄에 공백으로** 출력해야 함. 힌트대로 `print(값, end=" ")` 형태를 활용하면 줄바꿈 대신 공백을 넣을 수 있습니다.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
print(int(a < b), int(a == b))
@@TESTS
--IN
8 10
--OUT
1 0
--IN
15 15
--OUT
0 1
--IN
20 7
--OUT
0 0
@@EXPL
접근: 두 판정 결과(A<B, A==B)를 각각 1/0으로 만든 뒤 한 줄에 공백으로 붙여 출력하는 문제입니다. 비교식 결과를 정수로 바꾸는 감각과 한 줄 출력 형식이 핵심입니다.

코드 진행:

- 두 값을 읽어 정수 `a`, `b`로 만듭니다.
- `a < b`와 `a == b`는 각각 True/False가 되고, `int(...)`로 감싸면 1/0이 됩니다.
- `print(값1, 값2)`처럼 여러 값을 콤마로 넘기면 파이썬은 기본 구분자 공백을 넣어 `1 0`처럼 한 줄에 출력합니다.

다시 짤 때 생각 순서:

- 필요한 두 조건을 먼저 적습니다: "A가 B보다 작은가", "A와 B가 같은가".
- 각 조건을 1/0으로 바꾸는 방법(`int()`)을 떠올립니다.
- 한 줄 공백 출력은 `print(x, y)`의 기본 동작을 이용하거나 `end=" "`로도 만들 수 있음을 기억합니다.
```

### 6) 물의 상태 (Easy · 10XP · 정답률 70%)
- **요구사항**: 물의 온도에 따라 상태를 세 가지로 분류해 출력합니다.
- **입력**: 첫 줄에 정수 온도 n.
- **출력**: 0℃ 미만이면 `ice`, 100℃ 이상이면 `vapor`, 그 외에는 `water`.
- **예제**: `-13` → `ice`  |  `98` → `water`
- **제한**: −200 ≤ n ≤ 200 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: `if-elif-else`(3분기)
- **셀프체크 포인트**: 경계 조건에 주의 — 0은 `water`(미만이 아님), 100은 `vapor`(이상 포함). 조건 순서를 잘 배치해 세 구간이 겹치거나 빠지지 않게 하세요.

```runner
@@SOLUTION
n = int(input())
if n < 0:
    print("ice")
elif n >= 100:
    print("vapor")
else:
    print("water")
@@TESTS
--IN
-13
--OUT
ice
--IN
98
--OUT
water
--IN
100
--OUT
vapor
@@EXPL
접근: 온도를 세 구간(빙점 아래 / 끓는점 위 / 그 사이)으로 나누는 문제이므로 `if-elif-else` 3분기가 적합합니다. 경계값(0과 100)을 어느 구간에 넣을지가 핵심입니다.

코드 진행:

- `n`을 정수로 읽습니다.
- `if n < 0:`이면 `ice`. 0은 포함되지 않으므로 0은 여기서 걸리지 않습니다.
- `elif n >= 100:`이면 `vapor`. 100은 포함(이상)이라 `>=`를 씁니다.
- 위 두 조건이 모두 거짓인 나머지(0 이상 100 미만)는 `else`에서 `water`.

다시 짤 때 생각 순서:

- 세 구간의 경계를 먼저 그려 봅니다: `< 0`, `0 이상 100 미만`, `100 이상`.
- 각 경계값(0, 100)이 어느 쪽에 속하는지 문제에서 확인해 `<`와 `>=`를 정확히 선택합니다.
- 가운데 구간은 조건을 따로 쓰지 않고 `else`로 처리하면 겹침/누락 없이 깔끔합니다.
```

### 7) 살 수 있는 물건 (Easy · 10XP · 정답률 77%)
- **요구사항**: 가진 돈으로 살 수 있는 물건 중 **가장 비싼 것**의 이름을 출력합니다. (book 3000원, mask 1000원)
- **입력**: 첫 줄에 가진 돈 n.
- **출력**: 살 수 있는 가장 비싼 물건의 이름(`book` 또는 `mask`). 아무것도 살 수 없으면 `no`.
- **예제**: `3000` → `book`  |  `1000` → `mask`  |  `0` → `no`
- **제한**: 0 ≤ N ≤ 10000 · Time 1000 ms · Memory 80 MiB
- **연습 개념**: `if-elif-else`, 비싼 것부터 판정하는 조건 순서
- **셀프체크 포인트**: 비싼 물건(book, 3000)을 먼저 검사해야 "가장 비싼 것"이 올바르게 선택됩니다. 각 기준값은 "이상"(`>=`) 판정. 어느 것도 못 사면 `no`.

```runner
@@SOLUTION
n = int(input())
if n >= 3000:
    print("book")
elif n >= 1000:
    print("mask")
else:
    print("no")
@@TESTS
--IN
3000
--OUT
book
--IN
1000
--OUT
mask
--IN
0
--OUT
no
--IN
2500
--OUT
mask
@@EXPL
접근: "살 수 있는 것 중 가장 비싼 것"을 고르는 문제라 비싼 물건부터 순서대로 검사하는 `if-elif-else`가 핵심입니다. 조건 순서를 잘못 두면 저렴한 물건이 먼저 선택됩니다.

코드 진행:

- 가진 돈 `n`을 정수로 읽습니다.
- `if n >= 3000:`을 가장 먼저 검사해 book(3000원)을 살 수 있으면 `book`을 출력합니다.
- 그 다음 `elif n >= 1000:`으로 mask(1000원)를 확인합니다. 여기 도달했다는 건 이미 3000 미만이라는 뜻입니다.
- 둘 다 못 사면 `else`에서 `no`를 출력합니다. 각 기준은 "이상"이라 `>=`를 씁니다.

다시 짤 때 생각 순서:

- 물건을 가격 내림차순으로 정렬해 둡니다(book 3000 → mask 1000).
- 비싼 것부터 `if`/`elif`로 검사하면 처음 참이 되는 것이 곧 "가장 비싼 살 수 있는 물건"입니다.
- 경계값은 "이상"이므로 `>=`, 아무것도 못 사는 경우를 `else`로 마무리합니다.
```

### 8) 등급 매기기 (Easy · 10XP · 정답률 87%)
- **요구사항**: 시험 점수를 입력받아 구간별 등급(A~F)을 출력합니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: 90 이상 A, 80 이상 B, 70 이상 C, 60 이상 D, 60 미만 F.
- **예제**: `91` → `A`  |  `76` → `C`
- **제한**: 0 ≤ N ≤ 100 · Time 1000 ms · Memory 64 MiB
- **연습 개념**: `if-elif-else`(다중 구간 분기)
- **셀프체크 포인트**: 높은 등급부터(`>= 90` → `>= 80` → …) 순서대로 검사해야 함. 각 경계는 "이상"이며, 마지막은 `else`(60 미만)로 F를 처리하세요.

```runner
@@SOLUTION
n = int(input())
if n >= 90:
    print("A")
elif n >= 80:
    print("B")
elif n >= 70:
    print("C")
elif n >= 60:
    print("D")
else:
    print("F")
@@TESTS
--IN
91
--OUT
A
--IN
76
--OUT
C
--IN
60
--OUT
D
--IN
59
--OUT
F
@@EXPL
접근: 점수 구간을 다섯 등급으로 나누는 전형적인 `if-elif-else` 다중 분기 문제입니다. 높은 등급부터 검사해야 각 구간이 정확히 하나만 걸립니다.

코드 진행:

- 점수 `n`을 정수로 읽습니다.
- `n >= 90`부터 위에서 아래로 검사합니다. 처음 참이 되는 조건 하나만 실행되고 나머지는 건너뜁니다.
- 예를 들어 76은 90/80은 거짓이고 `>= 70`에서 참이 되어 `C`가 나옵니다.
- 60 미만은 어떤 `elif`도 걸리지 않으므로 `else`에서 `F`를 출력합니다.

다시 짤 때 생각 순서:

- 각 등급의 하한선(90, 80, 70, 60)을 큰 값부터 나열합니다.
- 높은 기준부터 `if`/`elif`로 쌓으면, 낮은 조건이 위쪽 점수를 가로채지 않습니다.
- 모든 경계가 "이상"이므로 `>=`를 쓰고, 마지막 60 미만은 조건 없이 `else`로 처리합니다.
```

## ✅ 마스터 체크리스트

- [ ] 참/거짓으로 평가되는 조건식을 비교 연산자로 만들 수 있다
- [ ] `if` 블록의 들여쓰기 규칙을 이해하고 `IndentationError`를 피할 수 있다
- [ ] `if-else`로 두 갈래 분기를 구현할 수 있다
- [ ] `if-elif-else`로 3개 이상 다중 분기를 구현할 수 있다
- [ ] 조건의 경계값(`>=` vs `>`, 포함/미포함)을 정확히 설정할 수 있다
- [ ] 다중 분기에서 조건 순서(좁은 조건 → 넓은 조건)를 올바르게 배치할 수 있다
- [ ] `print(값, end=" ")`로 한 줄 공백 구분 출력을 만들 수 있다
- [ ] 정수 나눗셈 몫(`//`)과 나머지(`%`)를 조건과 함께 활용할 수 있다

## ⚠️ 자주 하는 실수

- **들여쓰기 누락/불일치**: `if`/`elif`/`else` 다음 줄을 들여쓰지 않으면 `IndentationError`. 같은 블록은 같은 들여쓰기 수준을 유지해야 합니다.
- **`=`와 `==` 혼동**: 같음 비교는 `==`. 조건식에 대입 연산자 `=`를 쓰면 오류가 납니다.
- **경계값 처리 오류**: "이상/이하"(포함)와 "초과/미만"(미포함)을 헷갈려 80점이 통과인지, 0℃가 ice인지 등을 잘못 판정하는 경우가 많습니다.
- **다중 분기 조건 순서 오류**: `if-elif-else`는 위에서부터 먼저 참이 되는 하나만 실행됩니다. 넓은 조건(예: 60 이상)을 위에 두면 90점도 D로 잘못 분류됩니다. 엄격한 조건을 먼저 배치하세요.
- **여러 `if` 남발**: 서로 배타적인 분기인데 `elif` 대신 별도 `if`를 나열하면 조건이 중복 실행되어 결과가 어긋날 수 있습니다.
- **출력 형식 불일치**: 줄바꿈 vs 공백 구분, 문자열 대소문자(`pass`, `ice`, `no` 등)를 문제 명세와 정확히 일치시키세요.
