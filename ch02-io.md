# Ch2. 입출력 — Codetree 101 스터디 가이드

> 언어 Python3 · 4개 레슨 · 개념 4 / 문제 11

## 📌 챕터 개요
`print()`의 출력 형식 조정(`sep`, `end`, f-문자열, 이스케이프 시퀀스)과 `input()`을 이용한 입력 처리(형 변환, 여러 줄 입력, `split()`, `map()`)를 배웁니다. 입력받은 값을 원하는 자료형으로 바꾸고, 계산 결과를 정해진 포맷(소수점 자릿수, 구분자 등)에 맞춰 출력하는 것이 핵심 목표입니다. 마지막에는 합·평균 계산 같은 실전 입출력 연습으로 마무리합니다.

## 📖 개념 정리 (레슨별)

### L1. 출력
**핵심 개념**
- `print()`는 여러 값을 한 번에 출력하며, 값 사이 구분자(`sep`)와 출력 끝 문자(`end`)를 바꿀 수 있습니다.
- f-문자열, 소수점 포맷, 이스케이프 시퀀스로 출력 형태를 자유롭게 다듬습니다.

**문법/사용법**
- `sep=` : 인수들 사이에 삽입할 구분자. 기본값은 공백 `' '`. `", "`, `"-"`, `""` 등으로 변경 가능.
- `end=` : 출력이 끝난 뒤 붙일 문자열. 기본값은 줄바꿈 `'\n'`. 공백이나 `""`로 바꾸면 다음 `print()`가 같은 줄에 이어짐.
- f-문자열 : 문자열 앞에 `f`를 붙이고 `{변수}` 또는 `{표현식}`을 넣어 값을 삽입.
- 소수점 포맷 : `{값:.nf}` → 소수점 n+1자리에서 반올림해 n자리까지 표시.
- 이스케이프 시퀀스 : `\n`(줄바꿈), `\t`(탭) 등.

**예시**
```python
print("2024", "04", "27", sep="-")          # 2024-04-27
print("사과", end=", "); print("바나나")     # 사과, 바나나
pi = 3.141592
print(f"파이: {pi:.2f}")                      # 파이: 3.14
print(f"1 + 1 = {1 + 1}")                     # 1 + 1 = 2
```

**주의·흔한 실수**
- `sep`과 `end`는 반드시 `print()`의 키워드 인자로 넣어야 함(위치 인자로 쓰면 그냥 출력됨).
- f-문자열에서 접두사 `f`를 빠뜨리면 `{변수}`가 문자 그대로 출력됨.
- `:.2f`는 반올림이므로 표시 자릿수만 바뀌고 원본 값은 그대로임을 기억.

### L2. 입력 입문
**핵심 개념**
- `input()`은 사용자로부터 한 줄을 받아오며, 결과는 **항상 문자열(str)**로 반환됩니다.
- 수로 쓰려면 `int()`, `float()`로 형 변환이 필요합니다.

**문법/사용법**
- `x = input()` : 한 줄 입력을 문자열로 받음.
- `input("메시지")` : 입력 전에 안내 메시지를 보여줌(프롬프트).
- `int(input())` / `float(input())` : 입력을 정수/실수로 변환.

**예시**
```python
age = int(input())
print("당신의 나이는", age, "살입니다.")

height = float(input())
print("당신의 키는", height, "cm입니다.")
```

**주의·흔한 실수**
- 형 변환 없이 문자열끼리 `+`하면 숫자 덧셈이 아니라 문자열 이어붙이기가 됨.
- 숫자가 아닌 문자를 `int()`로 변환하면 `ValueError` 발생(예: `int("123b")`).

### L3. 입력 (여러 줄·한 줄 다중 입력)
**핵심 개념**
- 여러 줄 입력은 줄마다 `input()`을 따로 호출합니다.
- 한 줄에 공백으로 여러 값이 오면 `input().split()`으로 나눠 받습니다.

**문법/사용법**
- 여러 줄 : `a = input()` 를 줄 수만큼 반복 호출.
- 한 줄 다중 : `a, b, c = input().split()` (공백 기준 분리, 모두 문자열).
- 형 변환 동반 : `a, b = map(int, input().split())` (분리된 값들을 한 번에 변환).

**예시**
```python
a, b, c = input().split()          # "apple banana cherry" → a,b,c

num1, num2 = map(int, input().split())
print("두 수의 합은", num1 + num2, "입니다.")   # "10 20" → 30
```

**주의·흔한 실수**
- 나열한 변수 개수와 입력 값 개수가 다르면 언패킹 오류 발생.
- `split()` 결과는 문자열 리스트 → 계산하려면 `int`/`float`/`map` 변환 필수.
- 여러 줄을 한 번에 못 받음. 줄마다 `input()`을 호출해야 함.

### L4. 입출력 연습
**핵심 개념**
- 입력받은 수로 합과 평균을 계산하는 실전 패턴을 익힙니다.
- 나눗셈 `/`의 결과는 실수(float)임을 이용해 평균을 구합니다.

**문법/사용법**
- 합 : `total = num1 + num2`
- 평균 : `average = total / 개수`
- 출력 포맷 : f-문자열 `{average:.2f}`로 소수 자릿수 지정.
- 거듭제곱 연산자 `**` : `a ** b`는 a의 b제곱. 예) `height ** 2`(제곱), `2 ** 10`→1024.

**연산자 활용 예시**
- 이 레슨은 변수와 연산자를 이용한 계산이 핵심 주제로, 합·평균 외에 거듭제곱 등 다양한 연산을 활용합니다.
- BMI 계산 예: `bmi = weight / (height ** 2)` → `float(input())`로 받은 값에 `/`와 `**`를 조합해 실생활 계산 수행.

**예시**
```python
num1, num2 = float(input()), float(input())
total = num1 + num2
print(f"합: {total}")
print(f"평균: {total/2:.2f}")
```

**주의·흔한 실수**
- 정수 나눗셈 `//`와 실수 나눗셈 `/`를 혼동하지 말 것(평균은 보통 `/`).
- 평균 출력 시 요구된 소수 자릿수 포맷을 정확히 맞춰야 함(예: `15` → `15.0`).

## 🧩 문제 채점용 정리

### 1) 변수 출력하기 3 (Easy · 10XP · 정답률 86%)
- **요구사항**: 변수 a, b, c에 각각 1, 2, 'C'를 넣고 지정 형식으로 출력.
- **입력**: 없음.
- **출력**: a, b, c 값을 순서대로 출력하되 사이에 `->`를 포함.
- **예제**: (입력 없음) → `1->2->C`
- **제한**: Time 5000 ms / Memory 288 MiB
- **연습 개념**: `print`의 `sep` 파라미터.
- **셀프체크 포인트**: `sep="->"`로 한 줄에 구분자 삽입이 되는가.

```runner
@@SOLUTION
a = 1
b = 2
c = 'C'
print(a, b, c, sep="->")
@@TESTS
--IN

--OUT
1->2->C
@@EXPL
**접근 방법**: 값 사이에 `->`를 끼워 한 줄로 출력하는 문제이므로, `print`의 `sep` 키워드 인자를 쓰면 됩니다.

**코드 진행 과정**

- 변수 `a`, `b`, `c`에 각각 `1`, `2`, `'C'`를 대입합니다. 여기서 `1`과 `2`는 정수, `'C'`는 문자열입니다.
- `print(a, b, c, sep="->")`처럼 세 값을 콤마로 나열하고, 값 사이 구분자를 `sep="->"`로 지정하면 `1->2->C`가 한 줄로 출력됩니다.

**스스로 재작성할 때 생각 순서**

- 먼저 "여러 값을 콤마로 나열하면 print가 기본적으로 공백으로 이어준다"를 떠올립니다.
- 그 공백을 `->`로 바꾸고 싶으니 `sep=`를 이용하면 된다는 결론에 도달합니다.
- `sep`은 반드시 키워드 인자로 넣어야 하며, 위치 인자로 쓰면 그냥 값처럼 출력된다는 점을 주의합니다.
```

### 2) 세 정수형 변수 선언 (Easy · 10XP · 정답률 87%)
- **요구사항**: 정수 변수 3개에 7, 23, 30을 대입하고 변수로 덧셈식을 완성해 출력.
- **입력**: 없음.
- **출력**: `7 + 23 = 30` 형식.
- **예제**: (입력 없음) → `7 + 23 = 30`
- **제한**: Time 5000 ms / Memory 288 MiB
- **연습 개념**: 변수 선언 + 출력 문자열 구성.
- **셀프체크 포인트**: 숫자와 기호(`+`, `=`) 사이 공백까지 정확히 맞췄는가.

```runner
@@SOLUTION
a = 7
b = 23
c = 30
print(a, "+", b, "=", c)
@@TESTS
--IN

--OUT
7 + 23 = 30
@@EXPL
**접근 방법**: 세 정수 변수를 선언하고, 변수와 기호를 섞어 `7 + 23 = 30` 형태의 식으로 출력하는 문제입니다. print의 기본 구분자(공백)를 활용합니다.

**코드 진행 과정**

- `a`, `b`, `c`에 각각 `7`, `23`, `30`을 대입합니다.
- `print(a, "+", b, "=", c)`처럼 변수와 문자열 기호를 콤마로 나열합니다. print는 각 값 사이에 기본 공백을 넣으므로 `7 + 23 = 30`이 그대로 완성됩니다.

**스스로 재작성할 때 생각 순서**

- 출력할 문장을 조각으로 나눕니다: `a`, `+`, `b`, `=`, `c`.
- 각 조각 사이에 공백이 하나씩 필요하므로, 콤마로 나열만 하면 print의 기본 sep(공백)이 그 역할을 해준다는 걸 이용합니다.
- 기호 `+`와 `=`는 문자열이므로 따옴표로 감싸야 한다는 점을 기억합니다.
```

### 3) 달에서 무게 구하기 (Easy · 10XP · 정답률 81%)
- **요구사항**: 추의 무게(13)와 달 중력 비율(0.165)을 변수에 저장, 곱셈식과 결과를 출력.
- **입력**: 없음.
- **출력**: 곱셈식과 결과 출력, 실수값은 모두 소수점 여섯째 자리까지 표현.
- **예제**: (입력 없음) → `13 * 0.165000 = 2.145000`
- **제한**: Time 5000 ms / Memory 288 MiB
- **연습 개념**: f-문자열 소수점 포맷(`:.6f`).
- **셀프체크 포인트**: `0.165`도 `0.165000`처럼 6자리로 채워 출력했는가.

```runner
@@SOLUTION
weight = 13
ratio = 0.165
result = weight * ratio
print(f"{weight} * {ratio:.6f} = {result:.6f}")
@@TESTS
--IN

--OUT
13 * 0.165000 = 2.145000
@@EXPL
**접근 방법**: 실수값을 항상 소수점 여섯째 자리까지 표시해야 하므로 f-문자열의 `:.6f` 포맷을 사용합니다.

**코드 진행 과정**

- `weight`에 `13`, `ratio`에 `0.165`를 저장하고, `result = weight * ratio`로 곱셈 결과 `2.145`를 구합니다.
- f-문자열 `f"{weight} * {ratio:.6f} = {result:.6f}"`에서 정수 `weight`는 그대로, `ratio`와 `result`는 `:.6f`로 소수 6자리까지 채워 출력합니다. 그 결과 `13 * 0.165000 = 2.145000`이 됩니다.

**스스로 재작성할 때 생각 순서**

- 출력 형식 `수 * 수 = 수`를 f-문자열 뼈대로 먼저 잡습니다.
- "모든 실수는 6자리까지"라는 조건을 보고, 실수가 들어가는 자리마다 `:.6f`를 붙입니다.
- `0.165`도 6자리로 채우면 `0.165000`이 된다는 점(뒤에 0을 채움)을 확인합니다.
```

### 4) 점수 입출력 (Easy · 10XP · 정답률 79%)
- **요구사항**: 점수 N을 입력받아 지정 포맷으로 출력.
- **입력**: 첫 줄에 정수 N (1 ≤ N ≤ 1000).
- **출력**: `Your score is N point.` 형식.
- **예제**: `143` → `Your score is 143 point.`
- **제한**: Time 1000 ms / Memory 64 MiB
- **연습 개념**: `int(input())`, 문자열과 변수 함께 출력.
- **셀프체크 포인트**: 마침표(`.`) 포함 여부, 공백 위치가 정확한가.

```runner
@@SOLUTION
n = int(input())
print(f"Your score is {n} point.")
@@TESTS
--IN
143
--OUT
Your score is 143 point.
--IN
1000
--OUT
Your score is 1000 point.
@@EXPL
**접근 방법**: 정수 하나를 입력받아 정해진 문장 안에 끼워 출력합니다. `int(input())`과 f-문자열을 씁니다.

**코드 진행 과정**

- `n = int(input())`으로 한 줄을 읽어 정수로 변환합니다.
- `print(f"Your score is {n} point.")`에서 `{n}` 위치에 값이 들어가고, 마침표까지 그대로 출력됩니다.

**스스로 재작성할 때 생각 순서**

- 고정된 문장 틀 `Your score is __ point.`을 먼저 적습니다.
- 빈칸에 들어갈 값이 입력받은 정수임을 확인하고 `{n}`으로 대체합니다.
- 문장 끝 마침표와 단어 사이 공백을 예제와 한 글자도 다르지 않게 맞춥니다.
```

### 5) 입력받아 계산 (Easy · 10XP · 정답률 90%)
- **요구사항**: 정수 a를 입력받아 a+2를 출력.
- **입력**: 첫 줄에 정수 a (1 ≤ a ≤ 100).
- **출력**: 계산 후 정수 값.
- **예제**: `18` → `20`
- **제한**: Time 1000 ms / Memory 80 MiB
- **연습 개념**: 입력 형 변환 후 산술 연산.
- **셀프체크 포인트**: 문자열 그대로 더해 `182`가 나오지 않도록 `int` 변환했는가.

```runner
@@SOLUTION
a = int(input())
print(a + 2)
@@TESTS
--IN
18
--OUT
20
--IN
100
--OUT
102
@@EXPL
**접근 방법**: 입력을 정수로 바꾼 뒤 2를 더해 출력하는 기본 산술 문제입니다.

**코드 진행 과정**

- `a = int(input())`으로 입력을 정수로 변환합니다. `int` 변환이 없으면 뒤의 `+`가 문자열 이어붙이기가 되어 버립니다.
- `print(a + 2)`로 `a`에 2를 더한 정수를 출력합니다. 입력 `18`이면 `20`이 나옵니다.

**스스로 재작성할 때 생각 순서**

- "입력값은 항상 문자열"이라는 사실을 떠올리고, 계산 전에 `int()`로 바꿉니다.
- 그 뒤 `+ 2` 연산을 하고 결과를 출력하면 끝입니다.
- 만약 `int`를 빼먹으면 `18 + 2`가 `182`(문자열 붙이기)가 됨을 기억해 실수를 방지합니다.
```

### 6) 실수 받아 그대로 출력 (Easy · 10XP · 정답률 86%)
- **요구사항**: 실수 N을 입력받아 소수점 둘째 자리까지 반올림 출력.
- **입력**: 첫 줄에 N (0 ≤ N ≤ 50).
- **출력**: N을 소수 둘째 자리까지 반올림.
- **예제**: `22.4567` → `22.46` / `15` → `15.00`
- **제한**: Time 1000 ms / Memory 80 MiB
- **연습 개념**: `float(input())`, `:.2f` 포맷.
- **셀프체크 포인트**: 정수 입력 `15`도 `15.00`으로 자릿수를 채웠는가.

```runner
@@SOLUTION
n = float(input())
print(f"{n:.2f}")
@@TESTS
--IN
22.4567
--OUT
22.46
--IN
15
--OUT
15.00
@@EXPL
**접근 방법**: 실수를 입력받아 소수 둘째 자리까지 반올림해 출력합니다. `float(input())`과 `:.2f` 포맷이 핵심입니다.

**코드 진행 과정**

- `n = float(input())`으로 입력을 실수로 변환합니다. 정수처럼 보이는 `15`도 `float`으로 받으면 `15.0`이 됩니다.
- `print(f"{n:.2f}")`에서 `:.2f`는 소수 셋째 자리에서 반올림해 둘째 자리까지 표시합니다. `22.4567`은 `22.46`, `15`는 `15.00`이 됩니다.

**스스로 재작성할 때 생각 순서**

- 값이 소수일 수 있으니 `float()`으로 변환합니다.
- "소수 둘째 자리까지"라는 요구를 `:.2f`로 옮깁니다.
- 정수 입력도 `:.2f`가 자동으로 `.00`을 채워준다는 점을 확인합니다.
```

### 7) 세 실수의 반올림 (Easy · 10XP · 정답률 83%)
- **요구사항**: 세 실수를 입력받아 각각 소수 셋째 자리까지 반올림 출력.
- **입력**: 한 줄에 하나씩 세 실수 a, b, c (0 ≤ a,b,c ≤ 1000).
- **출력**: 한 줄에 하나씩 소수 셋째 자리까지 출력.
- **예제**: `1.1278 / 85.21344 / 577.999564` → `1.128 / 85.213 / 578.000`
- **제한**: Time 1000 ms / Memory 64 MiB
- **연습 개념**: 여러 줄 입력(`input()` 반복), `:.3f` 포맷.
- **셀프체크 포인트**: 반올림이 자릿수 올림으로 이어지는 경우(577.999→578.000)도 처리되는가.

```runner
@@SOLUTION
a = float(input())
b = float(input())
c = float(input())
print(f"{a:.3f}")
print(f"{b:.3f}")
print(f"{c:.3f}")
@@TESTS
--IN
1.1278
85.21344
577.999564
--OUT
1.128
85.213
578.000
--IN
0
3.14159
999.9995
--OUT
0.000
3.142
1000.000
@@EXPL
**접근 방법**: 세 실수를 각각 다른 줄에서 입력받아, 각각 소수 셋째 자리까지 반올림해 한 줄에 하나씩 출력합니다. 여러 줄 입력이므로 `float(input())`을 세 번 호출합니다.

**코드 진행 과정**

- `a`, `b`, `c`를 각각 별도의 `float(input())`으로 받습니다. 값이 줄마다 하나씩 오기 때문입니다.
- 각 변수를 `print(f"{x:.3f}")`로 출력합니다. `:.3f`는 넷째 자리에서 반올림해 셋째 자리까지 표시하므로, `577.999564`는 자리올림이 연쇄되어 `578.000`이 됩니다.

**스스로 재작성할 때 생각 순서**

- "여러 줄 입력 = input()을 줄 수만큼 반복"을 떠올려 세 번 입력받습니다.
- 요구된 "소수 셋째 자리"를 `:.3f`로 옮깁니다.
- 반올림이 정수부까지 올라갈 수 있음(`999.9995`→`1000.000`)을 예상하고, 포맷만 맞으면 자동 처리됨을 확인합니다.
```

### 8) 입력받은 값 교체하기 (Easy · 10XP · 정답률 89%)
- **요구사항**: 정수 a, b를 입력받아 위치를 교환해 출력.
- **입력**: 첫 줄에 정수 a, b가 공백으로 구분 (1 ≤ a,b ≤ 100).
- **출력**: 교환된 a, b를 공백으로 구분해 출력.
- **예제**: `14 5` → `5 14`
- **제한**: Time 1000 ms / Memory 64 MiB
- **연습 개념**: `input().split()`, 값 스왑.
- **셀프체크 포인트**: 파이썬 다중 대입(`a, b = b, a`)으로 간단히 교환 가능한가.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
a, b = b, a
print(a, b)
@@TESTS
--IN
14 5
--OUT
5 14
--IN
100 1
--OUT
1 100
@@EXPL
**접근 방법**: 한 줄에 공백으로 들어온 두 정수를 나눠 받고, 두 변수의 값을 서로 바꿔 출력합니다. `input().split()`과 다중 대입 스왑을 씁니다.

**코드 진행 과정**

- `a, b = input().split()`로 한 줄을 공백 기준으로 나눠 두 변수에 담습니다(이 시점엔 문자열).
- `int()`로 각각 정수 변환합니다. (출력만 하는 문제라 사실 변환 없이도 되지만, 계산 습관을 위해 변환합니다.)
- `a, b = b, a`로 두 값을 한 번에 교환한 뒤 `print(a, b)`로 공백 구분 출력합니다.

**스스로 재작성할 때 생각 순서**

- "한 줄 다중 입력 = split()으로 분리"를 떠올립니다.
- 교환은 임시 변수 없이 `a, b = b, a` 한 줄이면 된다는 파이썬 특징을 활용합니다.
- 두 값을 콤마로 나열해 print하면 기본 sep(공백)이 자동으로 사이를 띄웁니다.
```

### 9) 입력받은 값과 합 출력 (Easy · 10XP · 정답률 89%)
- **요구사항**: 정수 A, B를 입력받아 A, B, A+B를 차례대로 출력.
- **입력**: 첫 줄에 정수 A, B가 공백으로 구분 (1 ≤ A,B ≤ 100).
- **출력**: A, B, 합을 차례대로 출력.
- **예제**: `5 6` → `5 6 11`
- **제한**: Time 1000 ms / Memory 80 MiB
- **연습 개념**: `map(int, input().split())`, 산술 후 함께 출력.
- **셀프체크 포인트**: 세 값을 한 줄에 공백으로 이어 출력했는가.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
print(a, b, a + b)
@@TESTS
--IN
5 6
--OUT
5 6 11
--IN
100 100
--OUT
100 100 200
@@EXPL
**접근 방법**: 두 정수를 입력받아 원래 두 값과 그 합을 한 줄에 나란히 출력합니다.

**코드 진행 과정**

- `a, b = input().split()`로 두 값을 분리한 뒤 각각 `int()`로 변환합니다. 합을 구하려면 반드시 정수여야 합니다.
- `print(a, b, a + b)`로 세 값을 콤마로 나열하면 공백으로 이어져 `5 6 11`이 출력됩니다.

**스스로 재작성할 때 생각 순서**

- 한 줄 다중 입력이므로 `split()`으로 나누고 각각 `int()` 변환합니다.
- 출력할 것은 `a`, `b`, 그리고 `a + b` 세 가지임을 정리합니다.
- 세 값을 콤마로 나열해 한 번의 print로 공백 구분 출력합니다.
```

### 10) 합과 평균 (Easy · 10XP · 정답률 80%)
- **요구사항**: 두 정수 A, B를 입력받아 합과 평균을 출력.
- **입력**: 첫 줄에 정수 A, B가 공백으로 구분 (1 ≤ A,B ≤ 100).
- **출력**: 첫 줄에 합과 평균을 공백으로 구분, 평균은 소수 첫째 자리까지.
- **예제**: `2 5` → `7 3.5` / `7 3` → `10 5.0`
- **제한**: Time 1000 ms / Memory 80 MiB
- **연습 개념**: 합·평균 계산, `:.1f` 포맷.
- **셀프체크 포인트**: 평균이 정수여도 `5.0`처럼 소수 첫째 자리까지 표시했는가.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
total = a + b
average = total / 2
print(total, f"{average:.1f}")
@@TESTS
--IN
2 5
--OUT
7 3.5
--IN
7 3
--OUT
10 5.0
@@EXPL
**접근 방법**: 두 정수의 합은 정수로, 평균은 소수 첫째 자리까지 실수로 출력합니다. 합·평균 계산과 `:.1f` 포맷을 씁니다.

**코드 진행 과정**

- `a, b`를 split 후 각각 `int()`로 변환합니다.
- `total = a + b`로 합을, `average = total / 2`로 평균을 구합니다. `/`는 결과가 항상 실수라서 `10 / 2`도 `5.0`이 됩니다.
- `print(total, f"{average:.1f}")`로 합(정수)과 평균(소수 1자리)을 공백으로 이어 출력합니다.

**스스로 재작성할 때 생각 순서**

- 입력을 나눠 정수로 바꾼 뒤 합과 평균을 각각 변수에 담습니다.
- 평균은 `//`가 아닌 `/`를 써야 소수부가 살아 있음을 기억합니다.
- "평균은 소수 첫째 자리까지"를 `:.1f`로 옮기고, 합은 그대로 정수 출력합니다.
```

### 11) 합과 평균의 차 (Easy · 10XP · 정답률 86%)
- **요구사항**: 세 정수의 합과 평균을 구하고, 합에서 평균을 뺀 값을 출력.
- **입력**: 첫 줄에 세 정수 a, b, c가 공백으로 구분 (1 ≤ a,b,c ≤ 100). 합은 3의 배수로 가정.
- **출력**: 1행 합, 2행 평균, 3행 (합 − 평균).
- **예제**: `10 3 5` → `18` / `6` / `12`
- **제한**: Time 1000 ms / Memory 80 MiB
- **연습 개념**: 세 값 합·평균, 정수 결과 출력.
- **셀프체크 포인트**: 합이 3의 배수라 평균이 정수이므로 소수점 없이(`6`) 출력해도 되는가 확인.

```runner
@@SOLUTION
a, b, c = input().split()
a = int(a)
b = int(b)
c = int(c)
total = a + b + c
average = total // 3
print(total)
print(average)
print(total - average)
@@TESTS
--IN
10 3 5
--OUT
18
6
12
--IN
1 2 3
--OUT
6
2
4
@@EXPL
**접근 방법**: 세 정수의 합과 평균을 구하고, 합에서 평균을 뺀 값까지 한 줄씩 출력합니다. 합이 3의 배수로 보장되므로 평균은 정수입니다.

**코드 진행 과정**

- `a, b, c = input().split()` 후 각각 `int()`로 변환합니다.
- `total = a + b + c`로 합을 구합니다.
- 합이 3의 배수라 평균이 딱 떨어지므로 정수 나눗셈 `//`를 써서 `average = total // 3`로 정수 평균을 만듭니다(소수점 없이 `6`).
- `total`, `average`, `total - average`를 각각 print해 세 줄로 출력합니다.

**스스로 재작성할 때 생각 순서**

- 세 값을 split으로 나눠 정수로 바꾼 뒤 합부터 계산합니다.
- 평균은 "합이 3의 배수"라는 조건 덕분에 소수점 없이 출력해야 하므로 `//`가 적절합니다.
- 마지막 줄은 앞서 구한 합과 평균을 그대로 빼면 되므로 `total - average`를 출력합니다.
```

## ✅ 마스터 체크리스트
- [ ] `print()`의 `sep=`로 값 사이 구분자를 바꿀 수 있다.
- [ ] `print()`의 `end=`로 같은 줄에 이어 출력할 수 있다.
- [ ] f-문자열 `{변수}`, `{표현식}`, `{값:.nf}`를 자유롭게 쓴다.
- [ ] 이스케이프 시퀀스 `\n`, `\t`의 출력 효과를 안다.
- [ ] `input()` 결과가 문자열임을 이해하고 `int`/`float`로 변환한다.
- [ ] 여러 줄 입력은 `input()`을 줄마다 호출한다.
- [ ] `input().split()`으로 한 줄 다중 입력을 나눠 받는다.
- [ ] `map(int, input().split())`으로 분리와 형 변환을 한 번에 한다.
- [ ] 소수 자릿수 포맷을 요구사항에 맞게 정확히 맞춘다.
- [ ] 합·평균 계산과 정수/실수 나눗셈 차이를 안다.

## ⚠️ 자주 하는 실수
- **형 변환 누락**: `input()` 값을 그대로 `+`해서 문자열 이어붙이기가 되는 경우(예: `18`+`2`→`182`가 아닌 `20`이 목표).
- **`sep`/`end`를 위치 인자로 전달**: 키워드(`sep=`, `end=`)로 넣지 않으면 그냥 출력됨.
- **f 접두사 누락**: `"{x}"`처럼 쓰면 값이 삽입되지 않고 중괄호가 그대로 나옴.
- **소수 자릿수 불일치**: `15` → `15.00`, 평균 `5` → `5.0`처럼 요구된 자릿수를 채우지 않음.
- **반올림 자리올림 간과**: `577.999564`를 셋째 자리로 반올림하면 `578.000`이 되는 경우를 놓침.
- **언패킹 개수 불일치**: `a, b = input().split()`인데 입력 값 개수가 다르면 오류.
- **정수/실수 나눗셈 혼동**: 평균에서 `//`를 써 소수부가 잘리는 실수.
- **출력 포맷 세부 누락**: 마침표, 공백, 화살표(`->`) 등 문자 하나까지 예제와 일치시켜야 채점 통과.
