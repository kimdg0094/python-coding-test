# Ch7. 문자열 — Codetree 101 스터디 가이드
> 언어 Python3 · 1개 레슨 · 개념 1 / 문제 3

## 📌 챕터 개요
문자열(str)은 문자를 나열해 만든 자료형으로, 파이썬에서 가장 자주 다루는 기본 타입 중 하나입니다.
이 챕터에서는 문자열의 생성, 인덱싱/슬라이싱, 연결·반복 연산, 주요 메서드(변환/공백제거/치환/분할/결합/검색/개수), 그리고 이스케이프 문자를 익힙니다.
학습 목표는 (1) 문자열이 불변(immutable)임을 이해하고 새 문자열을 만들어 조작하는 감각을 기르는 것, (2) 슬라이싱과 메서드를 조합해 원하는 부분을 뽑거나 가공하는 것, (3) 따옴표/줄바꿈 같은 특수 문자를 올바르게 출력하는 것입니다.

## 📖 개념 정리
### 문자열 (str)
**핵심 개념**:
- 문자열은 문자의 모음이며 작은따옴표(`' '`) 또는 큰따옴표(`" "`)로 감쌉니다. 여러 줄은 삼중 따옴표(`""" """`, `''' '''`)를 사용합니다.
- 문자열은 **불변(immutable)** 자료형입니다. 한 번 만들면 특정 위치의 글자를 직접 바꿀 수 없고, 바꾸려면 항상 "새 문자열"을 만들어야 합니다.
- 인덱스는 0부터 시작하며, 음수 인덱스(`-1`이 마지막)로 뒤에서부터 접근할 수 있습니다. 범위를 벗어나면 `IndexError`.

**문법/사용법**:
- 연결/반복: `+`로 이어붙이고, `*`로 반복합니다. (숫자와 바로 더할 수 없으므로 `str()`로 형변환 필요)
- 슬라이싱: `문자열[start:end:step]` — `start`는 포함, `end`는 제외. `start`/`end` 생략 가능, `step`에 음수를 주면 역순(`[::-1]`은 뒤집기).
- 주요 메서드:
  - 대소문자: `upper()`, `lower()`
  - 공백 제거: `strip()`(양쪽), `lstrip()`(왼쪽), `rstrip()`(오른쪽) — 공백뿐 아니라 `\n`, `\t`도 제거
  - 치환: `replace(찾을것, 바꿀것[, 횟수])` — 횟수 생략 시 전부 치환
  - 분할: `split([구분자])` — 인자 없으면 공백 기준으로 나눠 리스트 반환 (`input().split()`의 원리)
  - 결합: `"구분자".join(리스트)`
  - 검색: `find()`(없으면 `-1`), `index()`(없으면 `ValueError`)
  - 개수: `count(부분문자열)`
  - 포함 확인: `in` **연산자** (메서드 아님), `True`/`False` 반환
- 이스케이프 문자: `\n`(줄바꿈), `\t`(탭), `\\`(백슬래시), `\'`(작은따옴표), `\"`(큰따옴표).
- f-문자열(f-string): 문자열 앞에 `f`를 붙이고 `{ }` 안에 변수·식을 넣어 값을 문자열에 끼워 넣음. 동적 문자열 생성에 유용 (예: `f"합은 {a + b}입니다"`). 뒤 챕터의 출력 포매팅에서 자주 쓰임.

**예시**:
```python
text = "Python is fun"
print(text[1:4])      # yth  (end 제외)
print(text[::-1])     # 뒤집기: nuf si nohtyP
print(text.replace("fun", "cool"))   # Python is cool
print(" a,b ".strip().split(","))    # ['a', 'b']
print("-".join(["2026", "07", "01"]))  # 2026-07-01

# 서로 다른 따옴표로 감싸면 이스케이프 없이 따옴표 포함 가능
print('그는 말했다, "안녕"')          # 그는 말했다, "안녕"
```

**주의·흔한 실수**:
- `text[0] = "J"`처럼 인덱스로 값을 직접 대입하면 오류 — 불변이므로 새 문자열을 만들어야 함(예: 슬라이싱 조합, `replace`, 리스트 변환 후 재조립).
- 슬라이싱의 `end`는 **포함되지 않는다**는 점을 자주 놓침.
- `find`는 못 찾으면 `-1`, `index`는 예외 발생 — 상황에 맞게 선택.
- `in`은 대소문자를 구분함(`"python" in "Python..."`은 `False`).

## 🧩 문제 채점용 정리
### 1) 따옴표 출력  (Easy · 10XP · 정답률 73%)
- **요구사항**: 지정된 한 문장을 그대로 출력한다.
- **입력**: 없음(입력이 주어지지 않음).
- **출력**: `He says "It's a really simple sentence".` 를 출력.
- **예제**: 입력 없음 → 출력 `He says "It's a really simple sentence".`
- **제한**: Time 5000 ms / Memory 288 MiB
- **연습 개념**: 문자열 출력, 따옴표(작은/큰) 동시 포함, 이스케이프 vs 다른 따옴표로 감싸기.
- **셀프체크 포인트**: 문장 안에 큰따옴표(`"`)와 작은따옴표(`'`)가 모두 들어 있음. 문자열을 감싸는 따옴표 종류를 무엇으로 할지, 필요하면 이스케이프(`\"`, `\'`)를 쓸지 정해야 함. 마침표(`.`)와 끝 위치, 공백까지 원문과 정확히 일치하는지 확인.

```runner
@@SOLUTION
print('He says "It\'s a really simple sentence".')
@@TESTS
--IN

--OUT
He says "It's a really simple sentence".
@@EXPL
**접근 방법**: 출력할 문장 안에 큰따옴표(`"`)와 작은따옴표(`'`)가 동시에 들어 있으므로, 문자열을 감싸는 따옴표와 겹치는 쪽만 이스케이프하면 된다.

**코드 진행 과정**

- 문자열을 작은따옴표(`'`)로 감쌌다. 그러면 안에 있는 큰따옴표(`"`)는 그대로 써도 문제없다.
- 감싸는 따옴표와 같은 작은따옴표(`It's`의 `'`)만 `\'`로 이스케이프한다.
- `print`로 한 줄 출력한다.

**다시 작성할 때 생각 순서**

- 먼저 감싸는 따옴표를 하나 고른다(작은따옴표 또는 큰따옴표).
- 문장 안에서 "감싼 것과 같은 종류"의 따옴표만 앞에 `\`를 붙여 이스케이프한다.
- 큰따옴표로 감쌌다면 반대로 `It's`는 그냥 두고 `"It's a ...".`의 큰따옴표들을 `\"`로 바꾸면 된다. 어느 쪽이든 마침표와 공백까지 원문과 똑같이 맞춘다.
```

### 2) 더 긴 문자열  (Easy · 10XP · 정답률 83%)
- **요구사항**: 두 단어 중 길이가 더 긴 단어와 그 길이를 출력한다(길이가 같으면 `same`).
- **입력**: 첫 줄에 길이 20 이하인 두 단어가 공백으로 구분되어 주어짐.
- **출력**: 첫 줄에 더 긴 문자열과 길이를 공백으로 구분해 출력. 두 단어 길이가 같으면 `same` 출력.
- **예제**:
  - 입력 `Happy Coding` → 출력 `Coding 6`
  - 입력 `Hello World` → 출력 `same`
- **제한**: Time 1000 ms / Memory 128 MiB · 단어 길이 ≤ 20
- **연습 개념**: `input().split()`으로 두 값 받기, `len()`으로 길이 비교, 조건 분기.
- **셀프체크 포인트**: 길이가 같을 때 `same`만 출력하는 분기를 빠뜨리지 말 것. 출력은 "단어 + 공백 + 길이" 형식이며, 길이가 다를 때 어느 쪽이 더 긴지(부등호 방향)만 헷갈리지 않으면 됨.

```runner
@@SOLUTION
a, b = input().split()
if len(a) > len(b):
    print(a, len(a))
elif len(b) > len(a):
    print(b, len(b))
else:
    print("same")
@@TESTS
--IN
Happy Coding
--OUT
Coding 6
--IN
Hello World
--OUT
same
--IN
apple kiwi
--OUT
apple 5
@@EXPL
**접근 방법**: 두 단어를 받아 `len()`으로 길이를 비교한다. 세 가지 경우(a가 김 / b가 김 / 같음)를 조건 분기로 나눈다.

**코드 진행 과정**

- `input().split()`으로 한 줄을 공백 기준으로 나눠 두 변수 `a`, `b`에 나눠 담는다.
- `len(a) > len(b)`이면 `a`와 그 길이를 공백으로 구분해 출력한다. `print(a, len(a))`처럼 콤마로 넘기면 값 사이에 공백이 자동으로 들어간다.
- `len(b) > len(a)`이면 `b`와 그 길이를 출력한다.
- 두 조건이 모두 아니면(길이가 같으면) `same`을 출력한다.

**다시 작성할 때 생각 순서**

- 먼저 두 단어를 어떻게 나눠 받을지 정한다(`split()` 결과를 두 변수로 언패킹).
- 비교는 "더 긴 쪽" 두 경우 + "같은 경우" 한 개, 총 3분기라고 먼저 그려 둔다.
- "단어 + 공백 + 길이" 출력은 `print(단어, len(단어))` 또는 f-문자열 `print(f"{단어} {len(단어)}")` 어느 쪽이든 된다. 같을 때 `same`만 출력하는 분기를 빠뜨리지 않도록 마지막 `else`를 꼭 채운다.
```

### 3) a로 채워넣기  (Easy · 10XP · 정답률 87%)
- **요구사항**: 주어진 문자열에서 앞에서 2번째 문자와 뒤에서 2번째 문자를 `'a'`로 바꾼 결과를 출력한다.
- **입력**: 첫 줄에 길이 10 이상 100 이하의 문자열이 주어짐.
- **출력**: 첫 줄에 변경된 문자열을 출력.
- **예제**:
  - 입력 `leebroscode` → 출력 `laebroscoae`
  - 입력 `aaaaaaaaaaaaaaa` → 출력 `aaaaaaaaaaaaaaa`
- **제한**: Time 1000 ms / Memory 80 MiB · 10 ≤ 길이 ≤ 100
- **연습 개념**: 인덱싱(앞 2번째 = 인덱스 1, 뒤 2번째 = 인덱스 -2), 문자열 불변성 → 새 문자열로 재구성.
- **셀프체크 포인트**: "앞에서 2번째"는 인덱스 1, "뒤에서 2번째"는 인덱스 -2. 문자열은 불변이므로 `s[1]='a'`처럼 직접 대입하면 오류 — 슬라이싱을 이어붙이거나 리스트로 바꿔 조립. 예제2처럼 이미 해당 위치가 `a`면 결과가 원문과 같을 수 있음. 두 위치가 겹치지 않는지(길이 10 이상이라 인덱스 1과 -2는 항상 다름) 확인.

```runner
@@SOLUTION
s = input()
result = s[0] + "a" + s[2:len(s) - 2] + "a" + s[len(s) - 1]
print(result)
@@TESTS
--IN
leebroscode
--OUT
laebroscoae
--IN
aaaaaaaaaaaaaaa
--OUT
aaaaaaaaaaaaaaa
--IN
abcdefghij
--OUT
aacdefghaj
@@EXPL
**접근 방법**: 문자열은 불변이라 인덱스 1과 -2 위치를 직접 바꿀 수 없다. 대신 "바꿀 자리를 뺀 나머지 조각들"을 슬라이싱으로 뽑아 `'a'`와 이어 붙여 새 문자열을 만든다.

**코드 진행 과정**

- `s[0]` — 맨 앞 글자(그대로 유지).
- `"a"` — 앞에서 2번째(인덱스 1) 자리에 넣을 글자.
- `s[2:len(s) - 2]` — 인덱스 2부터 뒤에서 2번째 직전까지 가운데 부분(그대로 유지). 슬라이싱의 끝은 포함되지 않으므로 `len(s) - 2`가 곧 "뒤에서 2번째 인덱스"이며 그 앞까지만 잘린다.
- `"a"` — 뒤에서 2번째(인덱스 `len(s) - 2`) 자리에 넣을 글자.
- `s[len(s) - 1]` — 맨 뒤 글자(그대로 유지).
- 이 다섯 조각을 `+`로 이어 붙이면 원하는 새 문자열이 완성된다.

**다시 작성할 때 생각 순서**

- 바꿔야 할 두 위치(인덱스 1, 인덱스 -2)를 먼저 표시한다.
- 그 두 위치를 기준으로 문자열을 "앞 / 바꿀 자리 / 중간 / 바꿀 자리 / 뒤" 다섯 토막으로 쪼갠다.
- 유지할 토막은 슬라이싱으로 뽑고, 바꿀 두 자리는 `"a"`로 넣어 `+`로 조립한다. 겹침 걱정은 없다(길이 10 이상이면 인덱스 1과 -2는 항상 떨어져 있음). 슬라이싱 대신 `list(s)`로 바꿔 `lst[1]='a'`, `lst[-2]='a'` 후 `"".join(lst)` 해도 같은 결과가 나온다.
```

## ✅ 마스터 체크리스트
- [ ] 문자열을 작은/큰/삼중 따옴표로 만들고 빈 문자열도 구분해 쓸 수 있다
- [ ] 문자열이 불변임을 이해하고, 수정 시 새 문자열을 만든다
- [ ] 양수/음수 인덱싱과 `[start:end:step]` 슬라이싱을 자유롭게 쓴다
- [ ] `+`(연결), `*`(반복)와 `str()` 형변환을 상황에 맞게 쓴다
- [ ] `upper/lower/strip/replace/split/join/find/index/count`와 `in`을 구분해 쓴다
- [ ] `\n \t \\ \' \"` 등 이스케이프 문자를 정확히 출력한다
- [ ] `input().split()`으로 여러 값을 받아 `len()`으로 비교·분기할 수 있다

## ⚠️ 자주 하는 실수
- 슬라이싱의 `end`가 포함된다고 착각(실제로는 제외).
- 불변 문자열에 인덱스로 직접 대입(`s[i] = ...`)해 오류 발생.
- 음수 인덱스 계산 실수: "뒤에서 2번째"를 `-2`가 아닌 `-1`이나 `len-2` 혼동.
- 따옴표 포함 문자열에서 감싸는 따옴표 선택/이스케이프를 잘못해 출력이 원문과 미묘하게 달라짐(공백·마침표 포함).
- 길이 비교 문제에서 "같을 때" 처리를 누락.
- `find`/`index`, `in`/`for _ in _`의 차이를 혼동.
