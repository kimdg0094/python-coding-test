# Ch8. 단순 반복문 2 — Codetree 101 스터디 가이드

> 언어 Python3 · 4개 레슨 · 개념 4 / 문제 10

## 📌 챕터 개요
반복문을 조건문·제어문과 결합해 "단순히 도는 것"을 넘어 흐름을 제어하는 법을 배웁니다. `for`/`while` 안에서 `if`로 요소를 걸러내고, `continue`/`break`로 특정 반복을 건너뛰거나 즉시 종료하며, `cnt`·`total` 같은 누적 변수로 개수를 세거나 합을 구합니다. 마지막으로 `while True` 무한 루프와 `break` 탈출, 반복 조건을 이용한 유동적 반복 제어를 익혀, 언제 끝날지 미리 알 수 없는 입력도 처리할 수 있게 됩니다.

## 📖 개념 정리 (레슨별)

### L1. 조건문이 포함된 반복문
**핵심 개념**
- 반복문으로 시퀀스를 순회하면서 매 반복마다 `if`로 조건을 검사해, 조건을 만족하는 요소에만 작업을 수행합니다(데이터 필터링·조건부 로직).
- 반복 안에서 `if-elif-else`로 다중 분류를, `and`/`or` 복합 조건으로 더 정교한 판별을, 중첩 `if`로 계층적 논리를 표현할 수 있습니다.

**문법/사용법**
```
for 변수 in 시퀀스:
    if 조건:
        실행할 코드
```
매 반복에서 `if` 조건이 참일 때만 내부 블록이 실행됩니다.

**예시**
```python
numbers = [10, -5, 0]
for num in numbers:
    if num > 0 and num % 2 == 0:
        print(f"{num}: 양수이면서 짝수")
    elif num > 0:
        print(f"{num}: 양수이면서 홀수")
    elif num < 0:
        print(f"{num}: 음수")
    else:
        print(f"{num}: 0")
```

**주의·흔한 실수**
- `if`가 반복문 밖으로 들여쓰기가 어긋나면 조건 검사가 매 반복이 아니라 한 번만 수행됩니다.
- `elif` 순서가 중요합니다. 넓은 조건을 먼저 두면 뒤의 세부 조건이 영원히 걸리지 않습니다.
- 짝/홀 판별은 `num % 2 == 0`(짝수) / `!= 0`(홀수)로, 음수·0 처리를 빠뜨리지 않도록 분기를 빠짐없이 나눕니다.

### L2. Continue Break
**핵심 개념**
- `continue`: 현재 반복만 건너뛰고 다음 반복으로 넘어갑니다(반복 자체는 계속).
- `break`: 반복문을 즉시 통째로 종료합니다.
- 둘 다 보통 `if` 조건과 함께 써서, "이런 경우엔 건너뛰기 / 여기서 멈추기"를 표현합니다.

**문법/사용법**
- `continue`는 이후 코드를 실행하지 않고 루프 맨 위로 되돌아갑니다.
- `break`는 루프를 빠져나와 루프 다음 문장으로 이동합니다.

**예시**
```python
numbers = [10, -5, 7, -3, 0, 12, -1]
for num in numbers:      # 양수만 출력
    if num <= 0:
        continue
    print(num)           # 10, 7, 12

for num in range(1, 6):  # 3을 만나면 종료
    if num == 3:
        break
    print(num)           # 1, 2
```

**주의·흔한 실수**
- `continue`를 쓸 때 카운터/누적 코드가 `continue` 뒤에 있으면 건너뛰어져 집계가 틀어집니다. 순서에 주의하세요.
- `break`는 가장 안쪽 반복문 하나만 종료합니다(중첩 루프에서 전체를 끝내지 않음).
- 조건 없이 무조건 `break`/`continue`를 두면 루프가 의미 없이 즉시 끝나거나 무한히 건너뜁니다.

### L3. 반복문에서의 변수 사용
**핵심 개념**
- 반복 밖에서 변수를 초기화하고, 반복 안에서 누적/갱신해 최종 결과를 얻는 패턴입니다.
- `cnt`(개수 세기): 조건을 만족할 때마다 1씩 증가.
- `total`(합계): 각 값을 계속 더함.
- 변수 이름은 자유지만 역할이 드러나는 이름을 쓰는 게 좋습니다.

**문법/사용법**
- 반드시 루프 **시작 전에** `cnt = 0` / `total = 0`으로 초기화합니다.
- 루프 안에서 `cnt += 1`, `total += num`으로 갱신합니다.

**예시**
```python
numbers = [2, 3, 4, 5, 6]
cnt = 0
total = 0
for num in numbers:
    total += num          # 합계 누적
    if num % 2 == 0:
        cnt += 1          # 짝수 개수
print(cnt, total)         # 3 20
```

**문법/사용법 — 한 줄 입력을 수 리스트로 변환**
- `input().split()`: 공백으로 구분된 한 줄 입력을 문자열 리스트로 쪼갭니다(예: `"1 2 5"` → `['1','2','5']`).
- 리스트 컴프리헨션 `[int(x) for x in input().split()]`: 쪼갠 각 조각을 `int()`로 변환해 정수 리스트를 한 번에 만듭니다. 이후 챕터에서 여러 수를 한 줄로 입력받을 때 자주 쓰는 관용구입니다.

**예시 — 입력 합계**
```python
numbers = [int(x) for x in input().split()]  # "1 2 5 10" → [1, 2, 5, 10]
total = 0
for num in numbers:
    total += num
print(total)  # 18
```

**주의·흔한 실수**
- 초기화를 루프 **안**에 두면 매 반복마다 0으로 리셋되어 결과가 항상 0 또는 마지막 값만 남습니다.
- 개수/합을 하나의 루프에서 함께 구할 수 있으니 불필요하게 여러 번 순회하지 않습니다.
- 입력을 정수로 처리해야 할 때 `int()` 변환을 잊으면 문자열이 이어붙거나 오류가 납니다.
- `cnt` 변수는 문자열 순회에도 그대로 적용됩니다(예: 특정 문자 등장 횟수 세기 `for char in s`).

### L4. 무한 루프
**핵심 개념**
- `while True`는 조건이 항상 참이라 스스로 끝나지 않는 무한 루프이며, 내부에서 `break`로 탈출합니다.
- 입력 개수가 미리 정해지지 않은 경우(특정 값이 나올 때까지 반복)에 특히 유용합니다.
- `break` 없이 `while 조건:`의 반복 조건 자체를 잘 설계해 종료시키는 방식도 자주 씁니다(유동적 반복 횟수).

**문법/사용법**
```
while True:
    ... 작업 ...
    if 종료조건:
        break
```
또는 조건형: `while num <= 0:` 처럼 조건이 거짓이 되면 자연히 종료.

**함께 나오는 문법·함수**
- `while ... else:` — 반복문에도 `else` 절을 붙일 수 있습니다. 루프가 `break` 없이 끝까지(조건 거짓으로) 정상 종료되면 `else` 블록이 실행되고, `break`로 빠져나오면 실행되지 않습니다(예: 로그인 시도 횟수 초과 처리).
- `//`(정수 나눗셈): 몫만 남기는 나눗셈(예: `n = n // 2`). `/`는 실수 결과라 정수 반복 제어에는 `//`를 씁니다.
- `문자열.lower()`: 문자열을 모두 소문자로 바꿔 대소문자 구분 없이 비교할 때 사용(예: `if s.lower() == "done":`).
- `float()`: 입력 문자열을 실수로 변환(정수뿐 아니라 소수 입력을 더할 때).

**예시**
```python
total = 0
while True:
    s = input()
    if s == "done":
        break
    total += int(s)
print(total)
```

**참고 — while-else: break 유무로 else 실행이 갈린다**
```python
# (A) break로 빠져나가면 else는 실행 안 됨
n = 3
while n > 0:
    print(n, end=" ")
    if n == 2:
        break
    n -= 1
else:
    print("정상 종료")
# 출력: 3 2      (break로 나갔으므로 else 미실행)

# (B) 조건이 거짓이 되어 정상 종료하면 else 실행됨
n = 3
while n > 0:
    print(n, end=" ")
    n -= 1
else:
    print("정상 종료")
# 출력: 3 2 1 정상 종료
```

**주의·흔한 실수**
- `break`(또는 종료 조건을 참으로 만드는 갱신)를 빠뜨리면 프로그램이 영원히 멈추지 않습니다.
- 조건형 while에서 루프 변수 갱신을 잊으면 조건이 바뀌지 않아 무한 루프가 됩니다.
- 종료 값(예: 0, exit)을 만난 순간에는 출력/집계하지 않고 바로 빠져나가야 하는지 순서를 확인하세요.

## 🧩 문제 채점용 정리

### 1) 100점 도달하기 (Easy · 10XP · 정답률 78%)
- **요구사항**: 점수에 따라 등급을 매깁니다(90↑ A, 80↑ B, 70↑ C, 60↑ D, 60미만 F). 주어진 N부터 100까지 1점씩 올려가며 각 점수의 등급을 순서대로 출력합니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: N부터 100까지 각 점수의 등급을 공백으로 구분해 한 줄에 출력.
- **예제**: `85` → `B B B B B A A A A A A A A A A A` / `94` → `A A A A A A A`
- **제한**: Time 1000 ms · Memory 128 MiB · `0 ≤ N ≤ 100`
- **연습 개념**: 반복문 + if-elif 다중 분기(L1)
- **셀프체크 포인트**: N과 100 양끝을 모두 포함(range 끝값 +1). 등급 경계(정확히 90/80/…)를 올바른 쪽에 넣었는지, 공백 구분 출력 형식.

```runner
@@SOLUTION
n = int(input())
result = ""
for score in range(n, 101):
    if score >= 90:
        g = "A"
    elif score >= 80:
        g = "B"
    elif score >= 70:
        g = "C"
    elif score >= 60:
        g = "D"
    else:
        g = "F"
    if score == n:
        result = g
    else:
        result = result + " " + g
print(result)
@@TESTS
--IN
85
--OUT
B B B B B A A A A A A A A A A A
--IN
94
--OUT
A A A A A A A
@@EXPL
**접근 개념**: 반복문 + `if-elif-else` 다중 분기(L1)로, N부터 100까지 각 점수를 순회하며 등급을 판정합니다.

**코드 진행 과정**

- `range(n, 101)`로 N부터 100까지(양끝 포함) 훑습니다. 끝값이 100이어야 하므로 `+1`을 해서 `101`을 씁니다.
- 각 점수마다 `if score >= 90 ... elif ...` 순서로 넓은 경계부터 아래로 내려가며 등급 문자를 정합니다. 90 이상이면 A, 그다음 80·70·60을 차례로 검사하고, 아무 데도 안 걸리면 `else`로 F입니다.
- 출력은 공백 구분 한 줄이라, 문자열 `result`에 이어 붙입니다. 첫 점수(`score == n`)일 때는 앞에 공백을 붙이지 않고, 이후에는 `" " + g`로 붙여 앞뒤 여분 공백이 생기지 않게 합니다.

**스스로 다시 짤 때 생각 순서**

- 먼저 "어디부터 어디까지 도는가"를 정한다 → N~100.
- 한 점수를 등급으로 바꾸는 규칙을 `elif`로 큰 경계부터 작성한다(순서를 뒤집으면 안 됨).
- 여러 값을 한 줄에 공백으로 붙이는 방법(첫 값만 특별 처리)을 정한다.
```

### 2) 짝수만 거꾸로 출력하기 (Easy · 10XP · 정답률 83%)
- **요구사항**: N개의 정수 중 짝수만, 입력 순서의 **역순**으로 출력합니다.
- **입력**: 첫 줄에 N, 둘째 줄에 N개의 정수(공백 구분).
- **출력**: 짝수만 역순으로 한 줄 출력.
- **예제**: `1 / 0` → `0` · `6 / 3 1 4 5 6 2` → `2 6 4` · `7 / 4 6 3 8 2 1 10` → `10 2 8 6 4`
- **제한**: Time 1000 ms · Memory 80 MiB · `1 ≤ N ≤ 100`, `0 ≤ 정수 ≤ 100`, 짝수 최소 1개 보장
- **연습 개념**: 반복문 + if 필터링(L1), 역방향 순회
- **셀프체크 포인트**: "짝수 필터"와 "역순" 두 가지를 함께 처리(역순 순회하며 짝수만 뽑기). 0도 짝수로 취급하는지 확인.

```runner
@@SOLUTION
n = int(input())
nums = [int(x) for x in input().split()]
result = ""
i = n - 1
while i >= 0:
    if nums[i] % 2 == 0:
        if result == "":
            result = str(nums[i])
        else:
            result = result + " " + str(nums[i])
    i -= 1
print(result)
@@TESTS
--IN
1
0
--OUT
0
--IN
6
3 1 4 5 6 2
--OUT
2 6 4
--IN
7
4 6 3 8 2 1 10
--OUT
10 2 8 6 4
@@EXPL
**접근 개념**: 반복문 + `if` 필터링(L1)에 "역방향 순회"를 결합합니다. 인덱스를 마지막부터 0까지 줄여 가며 짝수만 골라 출력합니다.

**코드 진행 과정**

- 첫 줄에서 개수 N, 둘째 줄에서 N개의 정수를 `nums` 리스트로 받습니다.
- `i`를 `n - 1`(마지막 인덱스)에서 시작해 `while i >= 0`로 돌리고, 매 반복 끝에 `i -= 1`로 하나씩 앞으로 이동합니다. 이렇게 하면 입력의 **역순**으로 값을 봅니다.
- 각 값 `nums[i]`이 짝수(`% 2 == 0`)일 때만 출력 문자열에 붙입니다. 0도 `0 % 2 == 0`이라 짝수로 취급됩니다.
- 공백 구분 한 줄 출력을 위해, `result`가 비어 있으면 그대로 첫 값을, 아니면 `" " + 값`으로 이어 붙입니다.

**스스로 다시 짤 때 생각 순서**

- "역순"이 필요하므로 앞에서부터가 아니라 뒤 인덱스부터 도는 while을 만든다(`i = n-1`, `i -= 1`).
- 그 안에서 짝수만 통과시키는 `if`를 둔다(0 포함 주의).
- 통과한 값들을 공백으로 이어 한 줄로 만든다(첫 값만 공백 없이).
```

### 3) 친근하지 않은 수 (Easy · 10XP · 정답률 80%)
- **요구사항**: 1이상 N이하 정수 중 2·3·5 중 어느 것으로도 나누어떨어지지 않는 수("친근하지 않은 수")의 개수를 셉니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: 친근하지 않은 수의 개수.
- **예제**: `10` → `2` (해당 수는 1, 7)
- **제한**: Time 1000 ms · Memory 80 MiB · `1 ≤ N ≤ 1000`
- **연습 개념**: continue/카운터(L2), 조건 필터(L1)
- **셀프체크 포인트**: 2·3·5 중 하나라도 나눠떨어지면 제외 → `if i%2==0 or i%3==0 or i%5==0: continue` 후 카운트. 범위에 N 포함.

```runner
@@SOLUTION
n = int(input())
cnt = 0
for i in range(1, n + 1):
    if i % 2 == 0 or i % 3 == 0 or i % 5 == 0:
        continue
    cnt += 1
print(cnt)
@@TESTS
--IN
10
--OUT
2
--IN
1
--OUT
1
@@EXPL
**접근 개념**: `continue`로 원치 않는 경우를 건너뛰고(L2), `cnt` 카운터(L3)로 남은 수의 개수를 셉니다.

**코드 진행 과정**

- `cnt = 0`으로 개수를 초기화하고 `range(1, n + 1)`로 1부터 N까지(양끝 포함) 순회합니다.
- 각 수 `i`가 2·3·5 중 **하나라도** 나눠떨어지면(`or`로 연결) "친근한 수"이므로 `continue`로 건너뜁니다.
- `continue`에 걸리지 않고 내려온 수만이 "친근하지 않은 수"이므로 `cnt += 1`을 합니다.
- N=10이면 1과 7만 살아남아 답은 2, N=1이면 1만 남아 1입니다.

**스스로 다시 짤 때 생각 순서**

- 세고 싶은 것과 반대인 "제외 조건"을 먼저 정의한다 → 2·3·5의 배수.
- 그 조건을 `or`로 묶어 참이면 `continue`로 스킵한다.
- 스킵되지 않은 것만 `cnt += 1`. 초기화는 반드시 루프 밖에서.
```

### 4) 1부터의 합 (Easy · 10XP · 정답률 66%)
- **요구사항**: 1부터 100까지 1씩 키우며 더하다가, 합이 처음으로 N 이상이 되는 순간 **그때 더해진 수**를 출력합니다.
- **입력**: 첫 줄에 정수 N.
- **출력**: 합이 N 이상이 되는 순간 더한 수.
- **예제**: `5` → `3` (1+2+3에서 5 이상 도달)
- **제한**: Time 1000 ms · Memory 80 MiB · `1 ≤ N ≤ 5000`
- **연습 개념**: 누적 total 변수(L3) + break 조기 종료(L2)
- **셀프체크 포인트**: "합"이 아니라 "그때의 수 i"를 출력. 조건 도달 즉시 `break`. `>=` 경계(N 이상) 처리.

```runner
@@SOLUTION
n = int(input())
total = 0
for i in range(1, 101):
    total += i
    if total >= n:
        print(i)
        break
@@TESTS
--IN
5
--OUT
3
--IN
1
--OUT
1
@@EXPL
**접근 개념**: `total` 누적 변수(L3)로 1부터 계속 더하다가, 목표에 도달하는 순간 `break`로 조기 종료(L2)합니다.

**코드 진행 과정**

- `total = 0`으로 합을 초기화하고 `range(1, 101)`로 i를 1부터 키웁니다.
- 매 반복에서 먼저 `total += i`로 현재 수를 더합니다.
- 그 직후 `if total >= n`을 검사해, 합이 처음으로 N 이상이 되면 **더한 수 i**(합이 아님)를 출력하고 `break`로 빠져나옵니다.
- N=5면 1→1, 2→3, 3→6(≥5)에서 i=3을 출력. N=1이면 첫 반복 total=1≥1이라 1을 출력.

**스스로 다시 짤 때 생각 순서**

- "누적하다가 조건 도달 시 멈춤"이 핵심 → total 초기화 후 루프.
- 더하기와 조건 검사의 순서에 주의: 더한 **뒤** 검사해야 "그때 더한 수"가 맞다.
- 출력 대상은 합이 아니라 반복 변수 i임을 잊지 말고, 도달 즉시 `break`.
```

### 5) A부터 B까지 짝수의 합 (Easy · 10XP · 정답률 90%)
- **요구사항**: 두 자연수 A, B를 받아 A부터 B까지의 짝수를 모두 더해 출력합니다.
- **입력**: 첫 줄에 정수 A, B(공백 구분).
- **출력**: A~B 구간 짝수의 합.
- **예제**: `2 5` → `6` · `5 5` → `0`
- **제한**: Time 1000 ms · Memory 64 MiB · `1 ≤ A ≤ B ≤ 100`
- **연습 개념**: 누적 total(L3), 조건 필터(L1)
- **셀프체크 포인트**: 양끝 A, B 포함. 구간에 짝수가 없을 때 합은 0. 시작을 짝수로 맞춰 2씩 더하는 방식도 가능.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
total = 0
for x in range(a, b + 1):
    if x % 2 == 0:
        total += x
print(total)
@@TESTS
--IN
2 5
--OUT
6
--IN
5 5
--OUT
0
--IN
1 10
--OUT
30
@@EXPL
**접근 개념**: `total` 누적 변수(L3)와 짝수 판별 `if`(L1)를 합쳐, 구간 안 짝수만 골라 더합니다.

**코드 진행 과정**

- 한 줄로 들어온 A B를 `split()`으로 나눠 각각 `int()`로 변환합니다.
- `total = 0`으로 합을 초기화하고 `range(a, b + 1)`로 A부터 B까지(양끝 포함) 훑습니다. 끝값 B를 포함하려고 `+1`을 씁니다.
- 각 수 `x`가 짝수(`% 2 == 0`)일 때만 `total`에 더합니다. 짝수가 하나도 없으면 total은 0으로 남습니다(예: `5 5`).
- `2 5` → 2+4=6, `1 10` → 2+4+6+8+10=30.

**스스로 다시 짤 때 생각 순서**

- 두 수를 한 줄에서 받아 정수 둘로 나눈다.
- 구간을 A~B(양끝 포함)로 도는 range를 만든다(`b + 1` 주의).
- 짝수만 통과하는 `if` 안에서 total에 더하고, 초기값 0 덕분에 짝수가 없어도 0이 출력됨을 확인한다.
```

### 6) 배수의 개수 (Easy · 10XP · 정답률 89%)
- **요구사항**: 10개의 수를 받아 3의 배수 개수와 5의 배수 개수를 각각 셉니다.
- **입력**: 10개의 수가 한 줄에 하나씩.
- **출력**: 첫 줄에 (3의 배수 개수) (5의 배수 개수)를 공백 구분해 출력.
- **예제**: `7 42 10 54 34 55 57 60 30 50` → `5 5`
- **제한**: Time 1000 ms · Memory 64 MiB · `1 ≤ 수 ≤ 100`
- **연습 개념**: 두 개의 cnt 변수(L3), if 판별(L1)
- **셀프체크 포인트**: 카운터 2개를 각각 초기화·증가. 15처럼 3과 5 둘 다의 배수는 양쪽 모두에 세는지(if를 독립적으로) 확인.

```runner
@@SOLUTION
c3 = 0
c5 = 0
for _ in range(10):
    x = int(input())
    if x % 3 == 0:
        c3 += 1
    if x % 5 == 0:
        c5 += 1
print(c3, c5)
@@TESTS
--IN
7
42
10
54
34
55
57
60
30
50
--OUT
5 5
--IN
1
2
3
4
5
6
7
8
9
15
--OUT
4 2
@@EXPL
**접근 개념**: 서로 독립적인 `cnt` 변수 2개(L3)와 배수 판별 `if`(L1)를 씁니다.

**코드 진행 과정**

- `c3`, `c5`를 각각 0으로 초기화합니다.
- `range(10)`으로 정확히 10번 반복하며 한 줄씩 정수를 읽습니다.
- 두 조건을 `elif`가 아니라 **독립적인 두 개의 `if`**로 검사합니다. 그래야 15처럼 3과 5의 공배수가 양쪽 카운터 모두에 반영됩니다.
- 마지막에 `print(c3, c5)`로 두 개수를 공백 구분해 한 줄에 출력합니다.
- 둘째 케이스: 3의 배수는 3·6·9·15로 4개, 5의 배수는 5·15로 2개.

**스스로 다시 짤 때 생각 순서**

- 세야 할 값이 두 종류이므로 카운터를 두 개 준비한다.
- 10개 입력을 위해 `range(10)` 고정 반복을 쓴다.
- 두 판별을 `if` 두 개로 따로 둔다(같은 수가 둘 다에 세일 수 있으므로 `elif` 금지).
```

### 7) 문자열의 특정 위치 찾기 2 (Easy · 10XP · 정답률 88%)
- **요구사항**: `"apple", "banana", "grape", "blueberry", "orange"` 5개 문자열을 두고, 주어진 영문자 한 개가 각 문자열의 **세 번째 또는 네 번째 문자**와 일치하는 문자열을 찾아 출력하고, 그 개수도 출력합니다.
- **입력**: 첫 줄에 영문자 한 개.
- **출력**: 조건을 만족하는 문자열을 순서대로 한 줄에 하나씩 출력하고, 마지막 줄에 개수를 출력.
- **예제**: `a` → `banana` / `grape` / `orange` / `3` · `z` → `0`
- **제한**: Time 1000 ms · Memory 80 MiB
- **연습 개념**: 반복 + if 필터(L1), cnt(L3), 인덱싱
- **셀프체크 포인트**: 인덱스는 0부터이므로 3·4번째 문자는 `s[2]` 또는 `s[3]`. 일치하는 문자열이 없으면 개수 0만 출력. 세 번째/네 번째 중 하나라도 맞으면 채택.

```runner
@@SOLUTION
words = ["apple", "banana", "grape", "blueberry", "orange"]
c = input()
cnt = 0
for w in words:
    if w[2] == c or w[3] == c:
        print(w)
        cnt += 1
print(cnt)
@@TESTS
--IN
a
--OUT
banana
grape
orange
3
--IN
z
--OUT
0
--IN
e
--OUT
blueberry
1
@@EXPL
**접근 개념**: 리스트 순회 + `if` 필터(L1), `cnt`(L3), 그리고 문자열 인덱싱을 함께 씁니다.

**코드 진행 과정**

- 5개 단어를 고정 리스트 `words`로 두고, 입력에서 찾을 문자 `c`를 받습니다.
- `cnt = 0`으로 개수를 초기화합니다.
- 각 단어 `w`에 대해 **세 번째 문자 `w[2]`** 또는 **네 번째 문자 `w[3]`**가 `c`와 같은지 `or`로 검사합니다(인덱스는 0부터라 3·4번째가 [2]·[3]).
- 조건을 만족하면 그 단어를 즉시 출력하고 `cnt += 1`. 모든 단어를 본 뒤 마지막에 개수 `cnt`를 출력합니다.
- `a`: banana([2]='n'/[3]='a')·grape([3]='p'? [2]='a')·orange([2]='a') → 3개. `z`: 아무 것도 없어 0만. `e`: blueberry([3]='e') 1개.

**스스로 다시 짤 때 생각 순서**

- 검사할 단어 목록과 찾을 문자를 준비하고 카운터를 0으로 둔다.
- 각 단어의 세·네 번째 문자를 `w[2]`, `w[3]`로 꺼내 `or`로 비교한다(1-based 착각 주의).
- 맞으면 단어를 출력하며 세고, 루프가 끝난 뒤 개수를 한 번 출력한다(없으면 0).
```

### 8) 0 나올때까지 출력 (Easy · 10XP · 정답률 90%)
- **요구사항**: 정수를 계속 입력받다가 0이 나오면 멈추고, 0 이전까지 입력된 수를 그대로 출력합니다.
- **입력**: 줄 수는 정해지지 않음. 각 줄에 정수 하나, 그중 최소 하나는 0.
- **출력**: 0이 나오기 전까지의 수를 한 줄에 하나씩 순서대로 출력.
- **예제**: `3 / 5 / 0` → `3` `5` · `3 / 2 / 4 / 6 / 0` → `3` `2` `4` `6`
- **제한**: Time 1000 ms · Memory 80 MiB · `0 ≤ 수 ≤ 100`
- **연습 개념**: while True + break(L4)
- **셀프체크 포인트**: 0을 만나면 출력하지 말고 바로 종료(입력 직후 검사). 입력 개수가 미정이므로 무한 루프 + break 구조.

```runner
@@SOLUTION
while True:
    x = int(input())
    if x == 0:
        break
    print(x)
@@TESTS
--IN
3
5
0
--OUT
3
5
--IN
3
2
4
6
0
--OUT
3
2
4
6
@@EXPL
**접근 개념**: 종료 시점이 미정인 입력이므로 `while True` + `break`(L4)를 씁니다.

**코드 진행 과정**

- `while True:`로 무한 반복을 시작합니다.
- 매 반복에서 정수 하나를 읽어 `x`에 담고, **먼저** `if x == 0`을 검사합니다. 0이면 출력하지 않고 즉시 `break`로 루프를 끝냅니다.
- 0이 아니면 `print(x)`로 그대로 출력합니다. 그래서 0 자신은 결과에 나오지 않습니다.
- `3 5 0` → 3, 5까지만 출력하고 0에서 종료.

**스스로 다시 짤 때 생각 순서**

- 몇 개가 들어올지 모르므로 개수 고정 for 대신 `while True`를 택한다.
- 입력을 받은 직후 종료값(0) 검사를 먼저 한다(검사 전에 출력하면 0이 새어 나감).
- 종료값이 아니면 처리(출력)하고 다음 반복으로.
```

### 9) A 부터 B 까지 (Easy · 10XP · 정답률 80%)
- **요구사항**: A에서 시작해 규칙대로 수를 바꾸며 출력하다가 B를 넘는 순간 종료합니다. 규칙: 홀수면 2배, 짝수면 3 증가. B 이하인 값까지만 출력.
- **입력**: 첫 줄에 정수 A, B(공백 구분).
- **출력**: A부터 순서대로 B를 넘기 전까지의 수를 공백 구분해 출력.
- **예제**: `2 13` → `2 5 10 13` · `3 13` → `3 6 9`
- **제한**: Time 1000 ms · Memory 128 MiB · `1 ≤ A ≤ B ≤ 20`
- **연습 개념**: while 조건 제어(L4), 홀짝 분기(L1)
- **셀프체크 포인트**: B와 같은 값은 출력 포함, B를 초과하면 출력 안 함(경계 `<= B`). 현재 값 출력 후 다음 값 계산 순서. 무한 루프 방지를 위해 매 반복 값 갱신 필수.

```runner
@@SOLUTION
a, b = input().split()
a = int(a)
b = int(b)
cur = a
result = str(cur)
while True:
    if cur % 2 == 1:
        cur = cur * 2
    else:
        cur = cur + 3
    if cur > b:
        break
    result = result + " " + str(cur)
print(result)
@@TESTS
--IN
2 13
--OUT
2 5 10 13
--IN
3 13
--OUT
3 6 9
@@EXPL
**접근 개념**: 반복 조건으로 흐름을 제어하는 `while`(L4)에 홀짝 분기(L1)를 결합합니다.

**코드 진행 과정**

- A B를 받아 정수로 만들고, 현재 값 `cur = a`로 시작합니다. 첫 값 A는 항상 출력되므로 `result`에 미리 넣습니다.
- `while True:` 안에서 **다음 값**을 규칙대로 계산합니다: `cur`가 홀수면 `cur * 2`, 짝수면 `cur + 3`.
- 새로 계산한 `cur`가 B를 **초과하면**(`cur > b`) 출력에 넣지 않고 `break`. B와 같은 값은 포함해야 하므로 경계는 `>` 입니다.
- 초과가 아니면 `result`에 공백과 함께 이어 붙이고 반복을 계속합니다. 매 반복 `cur`가 반드시 바뀌므로 무한 루프에 빠지지 않습니다.
- `2 13`: 2(짝→+3)5(홀→×2)10(짝→+3)13(홀→×2=26>13 종료) → `2 5 10 13`.

**스스로 다시 짤 때 생각 순서**

- 시작값 A는 무조건 출력되니 먼저 결과에 담는다.
- "현재 값으로 다음 값을 만들고 → 범위(≤B)면 채택, 넘으면 멈춤" 순서를 지킨다.
- 홀짝에 따라 갱신식이 다르므로 `if cur % 2 == 1 / else`로 나눈다. 매 반복 값이 변해 종료가 보장되는지 확인한다.
```

### 10) 특정 수 맞히기 (Easy · 10XP · 정답률 89%)
- **요구사항**: 정수 25 맞히기. 입력값이 25보다 작으면 `Higher`, 크면 `Lower`, 25면 `Good` 출력 후 종료합니다.
- **입력**: 줄 수 미정. 한 줄에 정수 하나씩, 25가 최소 한 번은 나옴.
- **출력**: 각 입력에 대한 문자열을 한 줄에 하나씩 출력.
- **예제**: `27 / 10 / 25` → `Lower` `Higher` `Good`
- **제한**: Time 1000 ms · Memory 64 MiB · `1 ≤ 수 ≤ 100`
- **연습 개념**: while True + if-elif-break(L4, L2)
- **셀프체크 포인트**: 25일 때 `Good`을 출력한 뒤 반드시 `break`. 작을 때 `Higher`, 클 때 `Lower`(방향 헷갈리지 않기).

```runner
@@SOLUTION
while True:
    x = int(input())
    if x < 25:
        print("Higher")
    elif x > 25:
        print("Lower")
    else:
        print("Good")
        break
@@TESTS
--IN
27
10
25
--OUT
Lower
Higher
Good
--IN
25
--OUT
Good
@@EXPL
**접근 개념**: 종료 시점 미정 입력이라 `while True`(L4)로 돌리고, `if-elif-else` 다중 분기 안에서 정답을 만나면 `break`(L2)로 끝냅니다.

**코드 진행 과정**

- `while True:`로 계속 입력을 받습니다.
- 각 정수 `x`를 목표 25와 비교합니다: `x < 25`면 `Higher`(더 큰 수로 올려야 함), `x > 25`면 `Lower`(더 작게), 그 외(정확히 25)면 `Good`을 출력합니다.
- 25(`else`)일 때만 출력 후 `break`로 루프를 종료합니다. Higher/Lower일 때는 다음 입력을 계속 받습니다.
- `27 10 25` → 27은 크니 Lower, 10은 작으니 Higher, 25에서 Good 출력 후 종료.

**스스로 다시 짤 때 생각 순서**

- 언제 끝날지 모르는 입력 → `while True` + 정답에서 `break`.
- 비교 방향을 정확히 잡는다: 입력이 목표보다 **작으면** 더 높여야 하니 Higher, **크면** Lower.
- 정답(25)일 때 출력하고 나서 반드시 `break`를 넣어 무한 반복을 막는다.
```

## ✅ 마스터 체크리스트
- [ ] `for`/`while` 안에서 `if`로 요소를 걸러 조건부로만 작업할 수 있다
- [ ] `if-elif-else`와 `and`/`or` 복합 조건, 중첩 `if`로 다중 분류를 구현할 수 있다
- [ ] `continue`로 특정 반복만 건너뛰고 다음으로 넘어갈 수 있다
- [ ] `break`로 조건 충족 시 반복을 즉시 종료할 수 있다
- [ ] `cnt` 변수로 조건을 만족하는 요소 개수를 셀 수 있다
- [ ] `total` 변수로 값을 누적해 합계를 구할 수 있다
- [ ] 누적 변수를 루프 시작 전에 올바르게 초기화한다
- [ ] `while True` + `break`로 종료 시점이 미정인 입력을 처리할 수 있다
- [ ] `while 조건:`의 반복 조건을 설계해 자연스럽게 종료시킬 수 있다
- [ ] 반복 변수를 매 반복 갱신해 무한 루프를 방지한다

## ⚠️ 자주 하는 실수
- 누적/카운터 변수를 루프 **안**에서 초기화해 매번 0으로 리셋되는 경우
- `continue` 뒤에 카운트/누적 코드를 두어 집계가 누락되는 경우
- `break`가 중첩 루프에서 안쪽 하나만 종료한다는 점을 잊고 전체가 멈춘다고 착각하는 경우
- `while True`에서 종료 조건(또는 변수 갱신)을 빠뜨려 프로그램이 멈추지 않는 경우
- 범위·경계 실수: `range` 끝값 미포함, `>=`/`>` 혼동, 종료 값(0·25·B)을 출력에 잘못 포함/제외
- 종료 신호(0, exit 등)를 만난 순간 먼저 검사하지 않고 출력/처리부터 해버리는 순서 오류
- 입력을 `int()`로 변환하지 않아 문자열 연결·비교 오류가 나는 경우
- 문자열 인덱스를 1부터로 착각(세 번째 문자는 `s[2]`)하는 경우
