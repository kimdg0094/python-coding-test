# Ch1. 기본 — Codetree 101 스터디 가이드
> 언어 Python3 · 4개 레슨 · 개념 4 / 문제 4

## 📌 챕터 개요
프로그래밍을 처음 시작할 때 필요한 기본기를 다룬다. 화면에 값을 내보내는 `print()`, 데이터를 담아두는 변수, 값의 종류를 규정하는 기본 자료형(int/float/bool), 그리고 값을 계산·비교·결합하는 연산자까지가 학습 대상이다.
학습 목표: (1) 문자열과 숫자를 올바르게 출력할 수 있다. (2) 변수에 값을 저장·변경·복사하는 흐름을 이해한다. (3) 자료형과 형 변환 규칙을 안다. (4) 산술/비교/논리/증감대입 연산자를 상황에 맞게 사용한다.

## 📖 개념 정리 (레슨별)

### L1. 출력 입문
**핵심 개념**: `print()`는 괄호 안에 넣은 값을 화면에 내보내는 함수다. 결과 확인, 디버깅, 메시지 표시에 쓰인다.
**문법/사용법**:
- 문자열은 따옴표로 감싼다. 큰따옴표 `" "`와 작은따옴표 `' '`는 동일하게 동작한다.
- 숫자(정수·실수)는 따옴표 없이 그대로 넣는다.
- `print()`는 출력 후 자동으로 줄바꿈(개행)한다. 즉 `print`를 한 번 호출할 때마다 한 줄이 만들어진다.
- `#` 뒤의 내용은 주석으로, 실행되지 않는다(설명·메모용).

**예시**:
```python
print("Hello, World!")   # 문자열 출력
print(100)                # 정수 출력 -> 100
print(3.14)               # 실수 출력 -> 3.14
print("첫 번째 줄")
print("두 번째 줄")       # 서로 다른 두 줄로 출력됨
# print("이 줄은 주석이라 실행 안 됨")
```
**주의·흔한 실수**:
- 문자열에 따옴표를 빠뜨리면 변수 이름으로 해석되어 오류가 난다.
- 숫자를 따옴표로 감싸면 "숫자 모양의 문자열"이 되어 이후 계산이 안 된다.
- 여러 줄을 한 번에 출력하려고 여러 값을 한 `print`에 몰아넣지 말 것. 줄 단위 출력이 필요하면 `print`를 여러 번 호출한다.

### L2. 변수
**핵심 개념**: 변수는 데이터를 저장하는 메모리 공간에 붙인 이름이다. 이름을 통해 저장된 값을 읽고 바꿀 수 있다.
**문법/사용법**:
- 대입 연산자 `=`는 수학의 등호가 아니라 "오른쪽 값을 왼쪽 변수에 저장하라"는 명령이다. (왼쪽=변수명, 오른쪽=값)
- 같은 변수에 다시 대입하면 값이 덮어써진다. 실행 도중 언제든 값 변경 가능.
- `type()`으로 변수의 자료형을 확인할 수 있다.
- 명명 규칙: 알파벳·숫자·밑줄(`_`) 사용 가능 / 숫자로 시작 불가 / 대소문자 구분(`age`와 `Age`는 다름) / 예약어(`for`, `if`, `else` 등) 사용 불가 / 의미 있는 이름 권장.

**주요 자료형 (미리 보기)**:

지금은 `int`/`float`/`bool`/`str`/`list` 다섯만 기억하면 충분하고, `set`/`tuple`/`dict`는 이름만 훑고 넘어가면 된다.

먼저 익힐 것 (핵심)

- 정수형 `int`: 소수점 없는 수. 예) `num = 12` — 바로 다음 L3에서 깊게 다룸
- 실수형 `float`: 소수점 있는 수. 예) `price = 3.14` — L3에서 다룸
- 불리언 `bool`: 참(`True`)/거짓(`False`). 예) `is_student = True` — L3에서 다룸
- 문자열 `str`: 따옴표로 감싼 문자들. 예) `fruit = "banana"` — Ch7에서 깊게 다룸
- 리스트 `list`: 여러 값을 순서대로 담는 묶음(값 변경 가능). 예) `nums = [1, 3, 5]` — Ch6에서 깊게 다룸

나중에 배울 것 (지금은 이름만)

- 튜플 `tuple`: 리스트와 비슷하지만 한 번 만들면 값 변경 불가. 예) `point = (1, 2)`
- 셋 `set`: 중복 없는 값들의 모음(순서 없음). 예) `colors = {"red", "blue"}`
- 딕셔너리 `dict`: 키-값 쌍의 모음. 예) `man = {"age": 20}`

`type(값)`으로 자료형을 확인할 수 있다. 예) `type(3.14)` → `<class 'float'>`

**예시**:
```python
a = 3
print(a)       # 3
a = 10         # 값을 덮어씀
print(a)       # 10

name = "홍길동"
is_student = True
print(type(a))         # <class 'int'>
print(type(name))      # <class 'str'>
```
**주의·흔한 실수**:
- `a = b`는 "a와 b가 같다"가 아니라 "b의 값을 a에 넣는다"이다. 방향(오른쪽→왼쪽)에 주의.
- 정의하지 않은 변수를 출력하려 하면 오류가 난다.
- 대소문자를 섞어 쓰면 서로 다른 변수로 취급되니 일관성 있게 작성.

### L3. 기본 자료형
**핵심 개념**: 자료형은 값의 종류와 처리 방식을 규정한다. 기본 3종은 정수형(int), 실수형(float), 불리언형(bool)이다.
**문법/사용법**:
- **int(정수형)**: 소수점 없는 정수(양수·음수·0). `10 + 3` → `13`.
- **float(실수형)**: 소수점 포함 실수. 과학적 표기법 지원(`1.5e2` → `150.0`, `2.5e-3` → `0.0025`).
- **bool(불리언형)**: `True` / `False`만 가짐. 조건·논리 판단에 사용.
- **형 변환**:
  - `int()`는 소수점 이하를 **버림(반올림 아님, 0 방향으로 절삭 = truncate)**: `int(3.14)` → `3`, `int(4.99)` → `4`, `int(-4.99)` → `-4`.
  - 이 "0 방향 잘라내기"는 floor(내림, 더 작은 정수 쪽)와 음수에서 갈린다: `int(-2.99)` → `-2`(0 쪽)이지만 내림은 `-3`(작은 쪽). 지금은 `int()`(truncate)만 기억하면 된다.
  - `float()`는 정수를 실수로: `float(5)` → `5.0`.
  - 정수 → bool: `0`만 `False`, 그 외 전부 `True` (`bool(-5)` → `True`).
  - bool → 정수: `int(True)` → `1`, `int(False)` → `0`.

**예시**:
```python
print(int(4.99))    # 4  (버림)
print(int(-2.99))   # -2 (0 방향 절삭)
print(float(5))     # 5.0
print(bool(0))      # False
print(int(True))    # 1
```
**주의·흔한 실수**:
- `int()`가 반올림이라고 착각하지 말 것. `int(4.99)`는 `5`가 아니라 `4`.
- 음수 절삭 방향에 유의: `int(-2.99)`는 `-3`이 아니라 `-2`.
- `bool(0)`만 `False`이고, `bool(-5)`처럼 0이 아닌 값은 전부 `True`.

### L4. 연산자
**핵심 개념**: 연산자는 값을 계산·비교·결합하거나 변수 값을 간결하게 갱신한다. 크게 산술 / 비교 / 논리 / 증감대입 4종.

**산술 연산자**

| 연산자 | 의미 | 예시 | 결과 |
|--------|------|------|------|
| `+` | 덧셈 | `10 + 3` | `13` |
| `-` | 뺄셈 | `10 - 3` | `7` |
| `*` | 곱셈 | `10 * 3` | `30` |
| `/` | 나눗셈(항상 실수) | `9 / 3` | `3.0` |
| `//` | 몫(정수) | `10 // 3` | `3` |
| `%` | 나머지 | `10 % 3` | `1` |
| `**` | 거듭제곱 | `10 ** 3` | `1000` |

- int와 float를 섞으면 자동으로 float 결과가 된다: `5 + 3.2` → `8.2`.

**비교 연산자** (결과는 항상 `True`/`False`)

| 연산자 | 의미 | 예시 | 결과 |
|--------|------|------|------|
| `==` | 같다 | `5 == 5` | `True` |
| `!=` | 다르다 | `5 != 3` | `True` |
| `<` `>` | 작다 / 크다 | `5 > 3` | `True` |
| `<=` `>=` | 이하 / 이상 | `5 <= 3` | `False` |

- 연쇄 비교 가능: `3 < a < 5`처럼 한 번에 범위 검사(`17 < age < 23`).

**논리 연산자**

| 연산자 | 의미 | 참이 되는 조건 |
|--------|------|----------------|
| `and` | 그리고 | 양쪽 모두 True |
| `or` | 또는 | 하나라도 True |
| `not` | 부정 | 반대로 뒤집음 |

**증감 대입 연산자** (`a = a + 3`을 `a += 3`으로 축약)

| 연산자 | 예시 (초깃값) | 결과 |
|--------|----------------|------|
| `+=` | `x = 10; x += 5` | `15` |
| `-=` | `x = 10; x -= 3` | `7` |
| `*=` | `x = 4; x *= 3` | `12` |
| `/=` | `x = 20; x /= 4` | `5.0` (float) |
| `//=` | `x = 20; x //= 3` | `6` |
| `%=` | `x = 20; x %= 6` | `2` |
| `**=` | `x = 2; x **= 3` | `8` |

**주의·흔한 실수**:
- `/`는 나눈 결과가 딱 떨어져도 항상 float를 준다(`9 / 3` → `3.0`, `3`이 아님). 정수 몫이 필요하면 `//`.
- `==`(비교)와 `=`(대입)을 혼동하지 말 것.
- `/=`는 결과가 float가 되지만 `//=`는 정수를 유지한다.

## 🧩 문제 채점용 정리

### 1) 2줄 출력 (Easy · 10XP · 정답률 81%)
- **요구사항**: `Hello`와 `World`를 각각 한 줄씩 출력하는 프로그램을 작성한다.
- **입력**: 없음.
- **출력**: 첫 줄에 `Hello`, 둘째 줄에 `World`.
- **예제**: 출력
  ```
  Hello
  World
  ```
- **제한**: Time 5000ms / Memory 288 MiB.
- **연습 개념**: `print()` 기본 출력, 자동 개행.
- **셀프체크 포인트**: 두 단어를 한 줄에 붙여 쓰지 않았는지, 대소문자(H, W 대문자)와 철자를 정확히 맞췄는지 확인. `print` 두 번 호출로 자연스럽게 줄바꿈이 되는지 점검.

```runner
@@SOLUTION
print("Hello")
print("World")
@@TESTS
--IN
--OUT
Hello
World
@@EXPL
`print()`는 괄호 안의 값을 화면에 출력하고 자동으로 줄바꿈한다. 그래서 두 줄을 만들려면 `print`를 두 번 호출하면 된다.

- `print("Hello")` → `Hello`를 출력하고 줄을 바꾼다.
- `print("World")` → 다음 줄에 `World`를 출력한다.

문자열은 반드시 따옴표로 감싸고, 대소문자(H, W)와 철자를 예제와 똑같이 맞춘다. "여러 줄을 한 번에 출력"하려 하지 말고, 줄이 나뉘어야 하면 `print`를 그 수만큼 나눠 호출한다고 생각하면 쉽다.
```

### 2) 문자 변경하기 (Easy · 10XP · 정답률 81%)
- **요구사항**: 변수 `a`에 먼저 `C`를 넣고, 그 뒤 다시 `a`에 `T`를 넣은 다음 `a`를 출력한다.
- **입력**: 없음.
- **출력**: 첫 줄에 변수 `a`의 최종 값.
- **예제**: 출력 `T`
- **제한**: Time 5000ms / Memory 288 MiB.
- **연습 개념**: 변수 대입과 값 덮어쓰기(재대입).
- **셀프체크 포인트**: 나중에 대입한 값이 이전 값을 덮어쓴다는 점이 핵심. 최종 출력이 `C`가 아니라 `T`가 되어야 함. 문자 값은 따옴표로 감싸야 함(문자열).

```runner
@@SOLUTION
a = "C"
a = "T"
print(a)
@@TESTS
--IN

--OUT
T
@@EXPL
이 문제는 **변수 재대입(덮어쓰기)** 개념을 확인한다. 같은 변수에 값을 다시 넣으면 이전 값은 사라지고 마지막 값만 남는다.

- `a = "C"` → 변수 `a`에 문자열 `"C"`를 저장한다.
- `a = "T"` → 같은 변수 `a`에 `"T"`를 다시 저장한다. 이 순간 앞의 `"C"`는 덮어써져 사라진다.
- `print(a)` → 현재 `a`의 값인 `T`를 출력한다.

스스로 다시 짤 때는 이렇게 생각한다.

- 대입은 항상 "오른쪽 값을 왼쪽 변수에 넣는 명령"이며, 나중 대입이 이긴다는 점을 떠올린다.
- 문자 하나라도 문자열이므로 반드시 따옴표로 감싼다.
- 출력은 마지막에 남은 값 기준이라는 것을 기억하고, `C`가 아니라 `T`가 나오는지 검산한다.
```

### 3) 변수 값 복사하기 3 (Easy · 10XP · 정답률 83%)
- **요구사항**: 정수 `a`, `b`, `c`에 각각 `1`, `5`, `3`을 넣고 → `c`의 값을 `a`에 복사 → `a`에는 `a + c`를, `b`에는 `b - c`를 대입 → 모든 연산 후 `a`, `b`, `c`를 차례로 출력한다.
- **입력**: 없음.
- **출력**: `a`, `b`, `c`를 한 줄에 하나씩(총 3줄).
- **예제**: 출력
  ```
  6
  2
  3
  ```
  (검산: `c=3`을 `a`에 복사 → `a=3`; `a = a + c = 3 + 3 = 6`; `b = b - c = 5 - 3 = 2`; `c=3` 그대로 → `6, 2, 3`)
- **제한**: Time 5000ms / Memory 384 MiB.
- **연습 개념**: 변수 복사, 순차적 대입, 연산 순서 추적.
- **셀프체크 포인트**: 연산 순서가 결과를 좌우한다. `c`를 `a`에 복사한 "이후"의 `a` 값으로 `a + c`를 계산해야 함(원래 `a=1`이 아님). `b - c`에 쓰이는 `c`는 여전히 `3`인지 확인.

```runner
@@SOLUTION
a = 1
b = 5
c = 3
a = c
a = a + c
b = b - c
print(a)
print(b)
print(c)
@@TESTS
--IN

--OUT
6
2
3
@@EXPL
이 문제는 **변수 복사와 순차 대입**을 손으로 추적하는 연습이다. 각 줄이 실행될 때 변수 값이 어떻게 바뀌는지 순서대로 따라가는 것이 핵심이다.

- `a=1, b=5, c=3`으로 초기화한다.
- `a = c` → `c`의 값 `3`을 `a`에 복사한다. 이제 `a=3` (원래의 `1`은 사라짐).
- `a = a + c` → 지금의 `a=3`과 `c=3`을 더해 `a=6`이 된다. 여기서 `a`는 반드시 복사 "이후"의 값이어야 한다.
- `b = b - c` → `5 - 3 = 2`이므로 `b=2`. 이때 `c`는 계속 `3`이다.
- `c`는 처음부터 끝까지 대입된 적이 없으므로 `3` 그대로다.
- 마지막에 `a`, `b`, `c`를 한 줄씩 출력하면 `6`, `2`, `3`.

스스로 다시 짤 때는 이렇게 생각한다.

- 종이에 `a b c` 세 칸을 그려두고, 한 줄 실행할 때마다 값을 지우고 새로 적는다.
- `a = c`처럼 복사가 나오면 "그 순간의 오른쪽 값"을 왼쪽에 넣는다는 것을 명심한다.
- 이후 계산은 항상 "지금 저장돼 있는 최신 값"으로 한다. 원래 초깃값(`a=1`)을 다시 쓰지 않는다.
```

### 4) 사칙연산 연습 (Easy · 10XP · 정답률 87%)
- **요구사항**: `a`에 `5`, `b`에 `30`을 넣고 둘의 합을 `c`에 저장한 뒤, `c`를 `8`로 나눈 **몫**과 **나머지**를 출력한다.
- **입력**: 없음.
- **출력**: 첫 줄에 몫, 둘째 줄에 나머지.
- **예제**: 출력
  ```
  4
  3
  ```
  (검산: `c = 5 + 30 = 35`; `35 // 8 = 4`, `35 % 8 = 3`)
- **제한**: Time 100ms / Memory 128 MiB.
- **연습 개념**: 산술 연산, 몫 `//`과 나머지 `%`의 구분.
- **셀프체크 포인트**: 몫에는 `/`가 아니라 `//`를 써야 함(`/`는 `4.375` 같은 실수가 나옴). 몫과 나머지의 출력 순서를 뒤바꾸지 않았는지 확인.

```runner
@@SOLUTION
a = 5
b = 30
c = a + b
print(c // 8)
print(c % 8)
@@TESTS
--IN

--OUT
4
3
@@EXPL
이 문제는 **몫 `//`과 나머지 `%`의 구분**을 연습한다. 두 수를 나눌 때 "몇 번 들어가는가(몫)"와 "남는 값(나머지)"을 각각 다른 연산자로 구한다.

- `a=5`, `b=30`을 저장하고 `c = a + b = 35`를 만든다.
- `c // 8` → `35`를 `8`로 나눈 정수 몫. `8 * 4 = 32`이고 `40`은 넘으므로 몫은 `4`.
- `c % 8` → 나눈 뒤 남는 나머지. `35 - 32 = 3`이므로 `3`.
- 몫을 먼저, 나머지를 그다음 줄에 출력한다.

스스로 다시 짤 때는 이렇게 생각한다.

- "몫이 필요하면 `//`, 나머지가 필요하면 `%`"를 먼저 확정한다. 실수 결과가 나오는 `/`는 여기서 쓰지 않는다.
- 합을 담는 중간 변수 `c`를 만들어 두면 식이 깔끔해진다.
- 출력 순서(몫 → 나머지)를 문제 요구대로 맞췄는지 마지막에 다시 확인한다.
```

## ✅ 마스터 체크리스트
- [ ] `print()`로 문자열과 숫자를 각각 올바른 형식(따옴표 유무)으로 출력할 수 있다.
- [ ] `print`의 자동 개행을 이해하고 여러 줄 출력을 만들 수 있다.
- [ ] 변수에 값을 대입하고, 재대입으로 값을 덮어쓸 수 있다.
- [ ] 변수 간 값 복사와 순차 연산의 결과를 손으로 추적할 수 있다.
- [ ] int / float / bool의 차이와 `type()` 확인법을 안다.
- [ ] `int()`(버림), `float()`, `bool()` 형 변환 규칙을 정확히 안다.
- [ ] `/`(실수)와 `//`(정수 몫), `%`(나머지)를 구분해 쓸 수 있다.
- [ ] 비교/논리/증감대입 연산자의 결과를 예측할 수 있다.

## ⚠️ 자주 하는 실수
- 숫자를 따옴표로 감싸 문자열로 만들어 버려 이후 계산이 안 되는 경우.
- 문자열의 따옴표를 빠뜨려 변수명으로 오인시켜 오류가 나는 경우.
- `int()`를 반올림으로 착각(`int(4.99)`는 `4`), 음수 절삭 방향 혼동(`int(-2.99)`는 `-2`).
- 몫을 구할 때 `//` 대신 `/`를 써서 실수 결과가 나오는 경우.
- `=`(대입)과 `==`(비교)를 혼동하는 경우.
- 변수 복사·순차 대입 문제에서 "복사 이후"가 아닌 원래 값으로 계산하는 순서 실수.
- 여러 줄 출력이 필요한데 한 줄로 붙여 출력하거나, 출력 순서를 뒤바꾸는 경우.
